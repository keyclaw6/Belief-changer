# Hypothesis — Iteration 011

source: hypothesizer (from 010 traces)

## Failure evidence

Voice-emotion KEEP object **factory-speech**: merged blocking **14** (replicate-a **6**, replicate-b **8**), down from **37** in 009 but not closed; merged noted **105** (was **120**). Belief and journey blocking **0**; book-arc blocking **0**. Blocking peaks still quote factory machinery inside assigned moments:

- replicate-b ch08: `This is the killer pair I promised you.` / `Future-pace this with me, because your next want is already scheduled …` / `That sentence is not reassurance theatre. It is the contract that makes this easy.`
- replicate-b ch09: `Warm rationale: any kept exception tells the Brainwashing its story …`
- replicate-a ch02: `That is the first valence.` / `So let me land the move we came to make, simply, flatly, warmly.`
- replicate-a ch06: `Watch how this collapses the FOR column entry.`
- replicate-a ch12: `That is doing TO you vs doing FOR you.`

Trap-question-label noted fell **34 → 7** under 010; **coach-register** noted **41 → 25**; **copied-mannerism** noted **23 → 30**. Secondary KEEP **instruction-paperwork** merged blocking **9** (was **7**) on unchanged plan strings — separate cluster.

## Root cause

Trace Analysis 010: iter-010’s writer-prompt silent-execution and translation-supremacy clause cut speakable craft-label blocking at former peaks, but the **runtime style-guide bundle** still primes exportable device names **below** that ban in every chapter `prompt.md`. `prompts/style-guide.md` §B5 operators **3, 6, 8, 11** teach `Trap question`, `Killer-line pair`, `Future-pacing`, and `warm rationale` as named delivery requirements; §B7 item **5** and §B9 checklist ask `killer-pair at the peak`. The model satisfies those checklist obligations at instruction blocks and argument peaks by pasting the labels (`killer pair I promised`, `Future-pace this with me`, `Warm rationale:`) — judges classify as blocking **factory-speech**. This is not a blind repeat of 007: 007 renamed labels (`trap question` → `disarming one-answer question`) while `prompts/chapter-writer.md` still required craft names; 010 removed that writer contradiction, but **style-guide was untouched**, so the bundle still contradicts the writer ban. Plan-ledger and frozen-token slug blocking (`FOR column`, `first valence`, FT-03 echo) remain plan-card-owned and are out of scope for this fix.

## Targeted fix

**File:** `prompts/style-guide.md` — one causal change: replace the canonical speakable-label operator instruction with silent-execution behavioral instructions, and delete exact duplicates in §B7 and §B9 that repeat the same `killer-pair` / `warm rationale` requirement.

**1.** Replace the §B5 header:

```text
## B5. The sentence-operator toolkit (execute silently — never surface operator names in reader prose)
```

**2.** Replace operators **3, 6, 8, 11** in §B5:

```text
3. **Concession question** — pose one question whose only honest answer concedes the point; perform it in flat Carr voice without naming, announcing, or labeling the question type.

6. **Peak verdict pair** — land two short mirrored sentences at the argument peak (build-up, then verdict); one major argument only; never name, count, promise, or announce the pair in reader prose.

8. **Upcoming-moment prediction** — predict the reader's specific upcoming thought, situation, and moment of revelation as lived experience; never coach-stage the prediction ("let me future-pace you", "future-pace this with me") or use factory labels as verbs.

11. **Instruction voice** — numbered, imperative, ALL-CAPS headline followed immediately by one short spoken rationale line; no "Warm rationale" header, craft label, or assignment-fulfillment narration around either line.
```

**3.** Replace §B7 item **5**:

```text
5. **Operator-rich, metric-true prose** per §B5 (questions ~10%, a silent peak verdict pair at the argument peak, ventriloquism where the reader would object, no hedging) — checklist metrics are silent-execution checks only; never surface craft labels in reader prose.
```

**4.** Replace the §B9 checklist line:

```text
- Question rate ~10%? A silent peak verdict pair at the argument peak? Sentences under 8 words present?
```

**Why this component:** Trace Analysis names the embedded style-guide bundle (§B5 operators 3/6/8/11, §B7 §5, §B9) as the **first remaining contradiction** exporting `killer pair`, `future-pacing`, and `warm rationale` into peaks after 010’s writer-prompt edit; subtracting those labels from the style-guide removes the runtime prime the writer ban cannot override when the model assembles instruction and peak blocks.

## Predicted impact

**What will improve:** Voice blocking **factory-speech** merged count falls in **both** replicates because peaks that currently paste checklist/operator vocabulary (`killer pair I promised`, `Future-pace this with me`, `Warm rationale:` headers, ease-operator scaffolding narration around assigned instructions) perform the move in flat Carr register instead. Noted **trap-question-label**, **coach-register**, and **factory-speech** noted counts should also fall as concession questions and upcoming-moment predictions stop exporting factory verbs.

**What might regress:** Plan-card ledger blocking (`FOR column`, `first valence`, FT-03 slug echoes) and **instruction-paperwork** (**9** merged blocking on unchanged plan safety strings) will likely persist. If silent operator wording is too vague, 1–2 chapters could lose a beat of peak compression without rising a new blocking class.

**How we'll know it worked:** The owning voice judge should no longer quote `killer pair`, `future-pace`, `Warm rationale:`, `reassurance theatre`, or `the contract` as blocking **factory-speech** in **both** replicates; merged blocking **factory-speech** should drop materially below **14** (plan-ledger lines may still block in ch02/ch06/ch12 until plan-card is addressed).
