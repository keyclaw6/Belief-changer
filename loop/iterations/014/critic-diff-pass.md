# Critic diff-pass 1 — 014 (KEEP-object only)

Applied diffs inspected: `git -C /home/kab/quit-sugar-iter-014 diff -- prompts/` (`chapter-writer.md`, `master-plan-skill-v2.md`, `master-plan-reviewer-v2.md`, `style-guide.md`). Consensus KEEP object = factory-speech blocking at CH-08 (card Voice / guardrail / primary-job operators). Extra inhabit / ITC / rooms hunks not scored here. MUST-NOTs held: CA-01 still boxed and unfused; **the Willpower Method** kept (Fork 4, mantra sheet, I-firewall, anti-method chapter); judges / PROGRAM / models / `research-agent.md` untouched.

## Will the next regenerated CH-08 card still be allowed to say those operators?

No. The compact-card contract no longer asks for a `Voice:` field. Primary job must name the belief correction, not a delivery register. Guardrails are safety-limit IDs and originality only. Reviewer blocks the 013 strings if they reappear. Absence of a `Voice:` field is an explicit non-blocker (R3), so the reviewer cannot demand the field back.

| 013 CH-08 speakable | Next card allowed? | Why |
|---|---|---|
| `Voice: hard truth flat` | No | P5: never a `Voice:` register/job operator. P8/P9: do not put `hard truth flat` as a craft note / do not create a `Voice:` operator. R2: block those operators. S4: compact cards carry reader-state and encounter, not a `Voice:` operator. |
| `deliver at full Carr force` on Voice / Guardrails / Primary job | No | P4: primary-job clause never a delivery register (`deliver at full Carr force`, `explicitly disown`, `hard truth flat`). P7: guardrails never speakable register/job operators. P8/P9/R2/S4 same strings. S1: do not copy Fork 3 onto card Voice or Guardrails. |
| `explicitly disown` on the card | No | S3 removed it from §9 Never (writer execution; do not paste as a Guardrails line). P4/P8/P9/R2 ban the string on cards. |
| don't-panic craft quote on Voice | No | S2 subtracted the §6 workshop quote. Remaining hits are ban-list mentions only (`workshop don't-panic quotes`). Nothing left to copy. |
| Maya as Reader | No | P1: book-core reader is a planner-facing state, not a vocative proper name, not a pupil-persona. P5: card reader-state is not a vocative proper name. R2: block a named pupil-persona (Maya or any proper-name handle) as the card reader. |
| CA-SAFE boxed title paste on the card | No | P3: cite the advisory ID only — never paste `CA-SAFE` / `CA-01` / `PRACTICAL SAFETY GUARDRAIL` or instruct the writer to box it mid-chapter. P7: never a boxed CA-SAFE/CA-01 title on guardrails. R2: block boxed titles as card paste instructions. S4/S5: not a paste of the boxed title. ID routing (`CA-SAFE` as a guardrail ID) stays. |

## Will the writer still paste the CA-SAFE header or address Maya?

No. W1: when a card routes CA-SAFE or CA-01, honour the limit silently or as the one spoken evidence clause — never paste the boxed workshop title mid-chapter, never add "this notice is not part of the belief argument." Address the reader as you/we only: never name a plan handle or pupil in vocative, third person, or IN THIS CHAPTER. S5: chapter prose never pastes the boxed workshop title. Assignment-fulfillment ban still outranks a leftover workshop string on a card.

## Leftover speakable seeds (grep of the four files)

Exact 013 card strings as **positive craft the planner could copy onto CH-08**: gone, except Fork 3 writer-force (intentional).

**Ban-list only (not seeds):** `hard truth flat`, `deliver at full Carr force`, `explicitly disown`, `workshop don't-panic quotes`, `Voice:` — in `master-plan-skill-v2.md` P4/P8/P9, `master-plan-reviewer-v2.md` R2, `style-guide.md` S4. `Maya` appears only in R2 as a string to block. `CA-SAFE` paste appears only as never-paste (P3/P7/W1/S4/S5).

**Writer-facing force left on purpose (S1/S2/S3 GO as written — keep force, do not copy onto cards):**

- `style-guide.md` Fork 3 DEFAULT: "Deliver the hard facts at full Carr force — vivid, flat…" plus "Do not copy this operator onto chapter-card Voice or Guardrails fields."
- `style-guide.md` §6: heading **Hard truth, then relief.** plus "This is writer execution, not a card Voice/guardrail string." Don't-panic quote gone.
- `style-guide.md` §9 Never: scare then re-land on escape-joy; "Writer execution — do not paste this as a card Guardrails line."
- `style-guide.md` §12 checklist: "land at full Carr force and then get disowned" — writer pre-flight, not a card field. Normalization already forbids copying generic style-guide rules into every card.
- `master-plan-skill-v2.md` method sentence: "fear deployed the corpus way — raised at full force, then disowned by the escape — wherever the plan assigns it" — method, not a `Voice:` / Guardrails / primary-job operator. P9 still forbids a `scare-then-disown` field and spoken-fear-disown line on cards.

**Residual, not KEEP-object reopen:** B8 item 6 is still titled "The reader handle" (description is planner-facing; compact cards are reader-state). P5 bans vocative proper name on the card, not every proper-name handle in isolation; R2 closes that by blocking Maya / any proper-name handle as the card reader. Writer W1 covers leak if a handle survived review.

No leftover `don't panic` / `you're addicted` quote. No leftover instruction to paste a boxed CA-SAFE title.

## KEEP-object checklist

1. **CH-08 Voice/Guardrails/Primary-job operators stripped from the card contract — PASS.** Compact cards: reader-state + encounter, never `Voice:`. Primary job = correction, never those three workshop jobs. Guardrails = IDs, never speakable operators or boxed title. Normalization + do-not-put list name the 013 strings.
2. **Reviewer will refuse them if they return — PASS.** R2 blocks Voice operators (those strings + don't-panic quotes), boxed CA-SAFE/CA-01 paste instructions, Maya as card reader. R3: do not block absence of a `Voice:` field.
3. **Style-guide seeds that 013 copied are either subtracted or tagged not-for-cards — PASS.** S2 quote gone. S1/S3 keep Carr force for the writer and forbid card paste.
4. **Writer cannot paste CA-SAFE header or address Maya even if a stale card survived — PASS.** W1 + S5. (Orchestrator must still regenerate the plan; leaving 013 CH-08 lines in the packet remains a no-op.)
5. **MUST-NOTs — PASS.** CA-01 box/content stays; Willpower Method stays; no judge/PROGRAM/model edits in this diff.

## Verdict: **GO**

A planner following only these prompts cannot lawfully put `Voice: hard truth flat`, `deliver at full Carr force`, `explicitly disown`, the don't-panic craft quote, Maya as Reader, or CA-SAFE boxed-title paste on the next CH-08 card. A writer following only these prompts will not paste the CA-SAFE header or address Maya. Leftover "full Carr force" / "Hard truth, then relief" lines are writer execution with an explicit do-not-copy-onto-cards rider, which is the KEEP-object fix (strip card operators, keep Carr force).

Regenerate the plan, then write two books.
