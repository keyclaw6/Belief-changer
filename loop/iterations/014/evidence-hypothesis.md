# Evidence hypothesis — iteration 014

**Founder override:** this is a BATCH like 013, not one-causal-change. Prefer subtraction. Do not paste GSBS copyrighted prose. Different level than 010–013 prompt-level silent-execution.

**KEEP objects this batch targets:** voice blocking `factory-speech` in BOTH books (013: A 2 / B 2, both at CH-08). Residual is plan-card Voice/guardrail operators at the fear hinge. T-A grafting is A-only hygiene, not the KEEP object.

**Downstream (orchestrator, not a hunk):** regenerate the plan after these prompt hunks. The writer packet is the accepted card. Leaving the 013 CH-08 Voice/guardrail lines in place would make the batch a no-op.

---

## Failure evidence

Unpaid 013 (KEEP residual), verified in census, decision, trace, and both `voice-emotion-ch08` FAILs:

1. **Blocking factory-speech 2/2 both books, both at CH-08.** Belief 14/14, journey 14/14, book-arc PASS, instruction-paperwork 0, compliance-missing 0 — hold. Do not reopen those.

2. **B (KEEP-class, both-books surface):** announces register/job at the fear hinge.
   - `"I will say this flat, warmly and without hedging, because softening it would keep you conned…"`
   - `"We delivered the hard facts at full strength because you deserve not to be conned."`
   Voice judge: primary-job MATERIAL; factory-speech blocking 2.

3. **A (same announcement class, unblocked by A's voice judge):** `"I say this next part flat, as it is, with no drama added…"` and `"I promised you I would deliver the hard truth at full force, and I have."` Trace: both books announce; only B blocked. This is the both-books residual.

4. **A-only T-A grafting (NOT the KEEP object):** `"close our eyes to for the rest of your life"` and `"If I am free for the rest of your life"`. B lands the same token as ordinary speech. Hygiene only.

5. **Noted factory-speech floor A 29 / B 28.** PERSISTENT 010 leftover → 011 leftover → 012 residual → 013 residual. Named 012 surfaces (trap-namer, wound-and-bandage, ledger, Warm rationale, P-02) are gone. Remaining textures: **CA-SAFE boxed header pasted mid-chapter** (identical in A and B ch08), evidence-grade asides. Prompt-level silent-execution is mutating the class. Different level required: strip speakable operators from cards, don't add another never-announce sentence.

6. **Book-arc PASS but noted re-argument 4 (A) / 8 (B):** settled scenes rebuilt (con trick, roller coaster, totality, sofa-tin). If touched: subtract card echo-of-full-scene, no new audit table.

7. **Maya named in reader prose** (plan human handle leaking as vocative): A `"Let us do this together, Maya"` / `"You know the Prison Door, Maya"`; B `"This is the heart of this chapter, Maya."`

Writer prompt already says never announce / perform silently / card workshop outranked. The CH-08 card still feeds speakable workshop:

```
* Voice: hard truth flat, then immediate relief ("don't panic — the trap is easy to break once you see it")
* Guardrails: hard facts, then relief — deliver at full Carr force and then explicitly disown fear as the motivator …
* Primary job: enacted transition — deliver stakes at full Carr force, then disown fear as motivator
* CA-SAFE safety limit routed here
```

Carr's matched move (described, not quoted): stacks facts, then relief, unannounced. Ours pre-labels "flat" delivery and recaps the job from outside the scene.

---

## Root cause

**Plan-card Voice/guardrail operators** (trace: plan-card, SIGNAL, both books). Style-guide §6 / Fork 3 / §9 craft phrasing (`hard truth flat`, `don't panic` workshop quote, `deliver at full Carr force`, `explicitly disown`) is copied onto cards because plan-skill still asks for "the voice or scene that makes the move land." Writer silent-execution cannot outrank a card that is itself speakable workshop.

**CA-SAFE box:** style-guide B10 + plan-skill still specify a boxed advisory; writers paste `**CA-SAFE — PRACTICAL SAFETY GUARDRAIL.**` as a mid-chapter workshop title. Safety must stay honored (012 CA-01 split: clinical tails stay out of instruction rows). The box title must not enter reader prose.

**PERSISTENT:** 010 writer-prompt, 011 style-guide, 012/013 packet hygiene — all KEEP-with-residual on factory-speech. 3+ times at prompt-ban level. This batch changes the **card contract**, not another writer never-announce.

---

## Targeted fix

Founder batch across the four editable prompt files. Subtraction first. Writer hunks are supporting (CA-SAFE paste, Maya vocative, T-A grammar) — **not** another never-announce sentence as the whole change.

Applier: each SEARCH below is unique in that file (verified). Apply in the worktree copies of these four files. Then regenerate the plan.

### MUST-NOT (do not implement)

- Reopen CA-01 split (do not fuse clinical tails into I-05/I-07/I-11).
- Ban the Willpower Method (T-D / I-06 / Fork 4 stay).
- Edit judges, PROGRAM, models, routes, `research-agent.md`.
- Kill `IN THIS CHAPTER`.
- Another hygiene-only never-announce writer sentence as the whole change.
- New re-argument audit table.
- Edit `production-books/` artifacts as the hypothesis.

---

### File 1 — `prompts/master-plan-skill-v2.md`

**P1 — Book core handle is planner-facing, not a vocative**

SEARCH:
```
Lock the target behavior, one reader named by a human handle (not P-xx persona codes), the load-bearing false belief, through-line, format, all five fork decisions, the redefinition and margin-for-error doctrine if needed, the clinical/eating-disorder safety perimeter, the strongest pro-behavior scene, the destination state, and one fresh ending reframe.
```

REPLACE:
```
Lock the target behavior, one reader named by a planner-facing human handle (not P-xx persona codes, never a vocative for chapter prose), the load-bearing false belief, through-line, format, all five fork decisions, the redefinition and margin-for-error doctrine if needed, the clinical/eating-disorder safety perimeter, the strongest pro-behavior scene, the destination state, and one fresh ending reframe.
```

**P2 — Scene debut-once; later cards token-echo only (re-argument subtraction, no audit table)**

SEARCH:
```
Chapter cards reference scene/analogy IDs and the job each performs; they never copy the full scene or analogy into the card.
```

REPLACE:
```
Chapter cards reference scene/analogy IDs. A scene debuts its full staging on exactly one card; later cards may cite that ID only as a one-phrase token echo, never a second staging or restated full job. They never copy the full scene or analogy into the card.
```

**P3 — Cite CA-SAFE/CA-01 by ID; do not paste the boxed title onto cards (CA-01 split stays)**

SEARCH:
```
Route it on non-argument safety cards and in evidence-ledger safety limits — not inside instruction frozen wording. When an instruction's belief job needs a qualified limit, state it once in plain spoken prose on that advisory; cards that assign epistemic-firewall instructions cite the advisory ID in their safety guardrails field only. Instruction peaks and verbatim recaps carry the bare imperative.
```

REPLACE:
```
Route it on non-argument safety cards and in evidence-ledger safety limits — not inside instruction frozen wording. When an instruction's belief job needs a qualified limit, state it once in plain spoken prose on that advisory; cards cite the advisory ID in their safety guardrails field only — they never paste the boxed workshop title (`CA-SAFE`, `CA-01`, `PRACTICAL SAFETY GUARDRAIL`) or instruct the writer to box it mid-chapter. Instruction peaks and verbatim recaps carry the bare imperative. Do not fuse clinical tails back into instruction rows.
```

**P4 — Primary job names the correction, not a delivery register**

SEARCH:
```
Declare that primary job as `enacted transition — <the correction completed now>`. Setup, topic coverage, a future-investigation prospectus, a catalogue for later demolition, or leaving the reader only willing to keep reading cannot be that job.
```

REPLACE:
```
Declare that primary job as `enacted transition — <the correction completed now>`. The clause names the belief correction, never a delivery register or workshop job (`deliver at full Carr force`, `explicitly disown`, `hard truth flat`). Setup, topic coverage, a future-investigation prospectus, a catalogue for later demolition, or leaving the reader only willing to keep reading cannot be that job.
```

**P5 — Subtract Voice: register/job operator from compact cards**

SEARCH:
```
- a human reader-handle (not P-xx) and the voice or scene that makes the move land;
```

REPLACE:
```
- a planner-facing reader-state (who they are at this beat — not a vocative proper name) and the concrete encounter that makes the move land; never a `Voice:` register/job operator;
```

**P6 — Later-card scene field is token-echo, not restage**

SEARCH:
```
- one or more concrete scene/analogy IDs and the argumentative job each performs;
```

REPLACE:
```
- one or more concrete scene/analogy IDs: debut card carries the staging job; later cards that cite the ID mark it as token-echo only;
```

**P7 — Guardrails are IDs, not speakable operators**

SEARCH:
```
- method, safety, and originality guardrails specific to this move;
```

REPLACE:
```
- method, safety, and originality guardrails specific to this move (safety-limit IDs and originality only — never speakable register/job operators, never a boxed CA-SAFE/CA-01 title);
```

**P8 — Card "do not put" list (the KEEP-class strip)**

SEARCH:
```
These are semantic authorities, not a prose template or mandatory chapter-section anatomy. Do not put device lists, persona codes, ALL-CAPS-peak inventories, or a `scare-then-disown` field name on cards.
```

REPLACE:
```
These are semantic authorities, not a prose template or mandatory chapter-section anatomy. Do not put device lists, persona codes, ALL-CAPS-peak inventories, a `scare-then-disown` field name, a `Voice:` register operator, `hard truth flat`, `deliver at full Carr force`, `explicitly disown`, or workshop don't-panic quotes as craft notes on cards.
```

**P9 — Normalization law (same strip; no duplicate behavior)**

SEARCH:
```
- a `scare-then-disown` field, spoken-fear-disown line, device list, or ALL-CAPS-peak inventory on cards;
```

REPLACE:
```
- a `scare-then-disown` field, spoken-fear-disown line, `Voice:` register/job operator (`hard truth flat`, `deliver at full Carr force`, `explicitly disown`, workshop don't-panic quotes), device list, or ALL-CAPS-peak inventory on cards;
```

---

### File 2 — `prompts/master-plan-reviewer-v2.md`

**R1 — Block restaged full scene-jobs**

SEARCH:
```
Block adjacent cards that repeat the same principal encounter, re-argue
settled work, break a handoff, or reserve work backward.
```

REPLACE:
```
Block adjacent cards that repeat the same principal encounter, re-argue
settled work, restage a debuted scene's full job (later cards token-echo
only), break a handoff, or reserve work backward.
```

**R2 — Block speakable Voice/register/job operators and boxed-title paste on cards**

SEARCH:
```
Block P-xx persona codes, unspeakable beat handles (device names,
"scare-then-disown", "Warm rationale"), and chapter-number ledger callbacks
on cards.
```

REPLACE:
```
Block P-xx persona codes, unspeakable beat handles (device names,
"scare-then-disown", "Warm rationale"), Voice: register/job operators
("hard truth flat", "deliver at full Carr force", "explicitly disown",
workshop don't-panic quotes), boxed CA-SAFE/CA-01 titles as card paste
instructions, and chapter-number ledger callbacks on cards.
```

**R3 — Absence of a Voice: field is not a blocker**

SEARCH:
```
Do not block the absence of a scare-then-disown field name, device lists,
persona codes, ALL-CAPS-peak inventory, or a per-chapter mantra assignment.
```

REPLACE:
```
Do not block the absence of a scare-then-disown field name, a Voice: field,
device lists, persona codes, ALL-CAPS-peak inventory, or a per-chapter mantra assignment.
```

---

### File 3 — `prompts/chapter-writer.md`

Supporting only. Do **not** add another "Never announce the move." That sentence already exists. These hunks extend existing Binding / evidence-honesty clauses.

**W1 — CA-SAFE box not pasted; handle not spoken as a name (extends existing never-surface)**

SEARCH:
```
  Honour those limits by not overclaiming. Never narrate study design, grades,
  or methods — in the body or in SUMMARY. Add one short spoken clause only
  when a hard fact would otherwise be taken as a sentence on this reader.
  Never surface workshop vocabulary: no ledger or source IDs, no
```

REPLACE:
```
  Honour those limits by not overclaiming. Never narrate study design, grades,
  or methods — in the body or in SUMMARY. Add one short spoken clause only
  when a hard fact would otherwise be taken as a sentence on this reader.
  When a card routes CA-SAFE or CA-01, honour that limit silently or as that
  one spoken clause — never paste the boxed workshop title (`CA-SAFE`,
  `CA-01`, `PRACTICAL SAFETY GUARDRAIL`) mid-chapter. The plan's human handle
  is planner-facing: address the reader as you/we; never vocative by that
  proper name. Never surface workshop vocabulary: no ledger or source IDs, no
```

**W2 — Frozen-token echo is a grammatical spoken sentence, not a graft (A T-A hygiene, not KEEP object)**

SEARCH:
```
2. When your card assigns a mantra or frozen token, land it exact in wording,
   capitalization, and punctuation. A debut gets its full argument-then-compression
   as lived reader experience, with no beat or device label; an echo is brief
   and is never re-argued. Invent and paraphrase none. A chapter with no
   assigned mantra is not a defect: do not invent one, and do not refuse.
```

REPLACE:
```
2. When your card assigns a mantra or frozen token, land it exact in wording,
   capitalization, and punctuation. A debut gets its full argument-then-compression
   as lived reader experience, with no beat or device label; an echo is a
   brief grammatical spoken sentence that contains the frozen token — never
   a graft of the token into a host clause that was not built to carry it,
   and never re-argued. Invent and paraphrase none. A chapter with no
   assigned mantra is not a defect: do not invent one, and do not refuse.
```

---

### File 4 — `prompts/style-guide.md`

Subtract the workshop quotes the planner copies onto Voice/Guardrails. Keep Carr force as writer execution.

**S1 — Fork 3: keep force; do not copy the operator onto cards**

SEARCH:
```
The fact does its perception work; the relief removes the loss-frame. Never announce the move. Never soften, hedge, or omit a scare Carr would have delivered — omission is how the factory drifts off-reference.
```

REPLACE:
```
The fact does its perception work; the relief removes the loss-frame. Never announce the move. Do not copy this operator onto chapter-card Voice or Guardrails fields. Never soften, hedge, or omit a scare Carr would have delivered — omission is how the factory drifts off-reference.
```

**S2 — §6: subtract the don't-panic workshop quote that landed on CH-08 Voice**

SEARCH:
```
- **Hard truth, then relief.** When you deliver a hard truth ("you're addicted"), immediately soothe it ("don't panic — the trap is easy to break once you see it"). Never leave the reader in dread. Never announce the move.
```

REPLACE:
```
- **Hard truth, then relief.** Deliver the hard fact, then in the same breath the relief. Never leave the reader in dread. Never announce the move. This is writer execution, not a card Voice/guardrail string.
```

**S3 — §9: keep scare-then-relief; do not paste as card Guardrails**

SEARCH:
```
- **Never *end* on fear — scare, then disown, exactly like Carr.** The hard material is delivered at full force (Carr frightens freely and flatly), and the chapter then explicitly disowns fear as the motivator and re-lands on escape-joy. Two failure modes, equally off-reference: leaving the reader quitting *out of* fear, and omitting or softening a scare Carr would have delivered.
```

REPLACE:
```
- **Never *end* on fear — scare, then disown, exactly like Carr.** The hard material is delivered at full force (Carr frightens freely and flatly), then fear is not left as the reason to change: the chapter re-lands on escape-joy. Writer execution — do not paste this as a card Guardrails line. Two failure modes, equally off-reference: leaving the reader quitting *out of* fear, and omitting or softening a scare Carr would have delivered.
```

**S4 — B8 reader handle: planner-facing; cards carry no Voice: operator**

SEARCH:
```
6. **The reader handle** — a human description of who this chapter speaks to (not P-xx codes), and the ventriloquism lines that make the move land.
```

REPLACE:
```
6. **The reader handle** — a planner-facing human description of who this chapter speaks to (not P-xx codes, never a vocative proper name in reader prose). Compact cards carry reader-state and encounter, not a `Voice:` register/job operator (`hard truth flat`, `deliver at full Carr force`, `explicitly disown`, workshop don't-panic quotes) and not a paste of the boxed CA-SAFE/CA-01 title.
```

**S5 — B10 boxed advisory is plan inventory; chapter prose does not paste the title**

SEARCH:
```
- **Practical-safety guardrail** (when the behavior touches medication/health): a boxed advisory routing medical specifics to a professional, kept outside the belief argument.
```

REPLACE:
```
- **Practical-safety guardrail** (when the behavior touches medication/health): a plan-wide boxed advisory (CA-01 split: clinical limits live here, never fused into instruction rows) routing medical specifics to a professional, kept outside the belief argument. Chapter prose honours it silently or as one spoken clause; never paste the boxed workshop title mid-chapter.
```

**Why these components:** The trace names plan-card as the CH-08 announcement root; reviewer must refuse the old Voice/guardrail fields or they return; style-guide is the seed the planner copies; writer Binding is the only place that can stop the CA-SAFE box, Maya vocative, and A-only T-A graft without another never-announce sentence.

---

## Predicted impact

| Signal | 013 A | 013 B | 014 prediction (both) |
|---|---|---|---|
| factory-speech **blocking** | 2 | 2 | **near 0** (KEEP object) |
| voice ch08 FAIL | T-A grafts | register/job announce | PASS if card operators gone; A's grafts are hygiene |
| noted factory-speech | 29 | 28 | drop (CA-SAFE box gone); evidence-grade asides remain |
| Maya vocative | present | present | gone |
| book-arc re-argument noted | 4 | 8 | modest drop if scene token-echo holds |
| belief / journey / book-arc | PASS | PASS | hold |
| instruction-paperwork | 0 | 0 | 0 (CA-01 unsplit) |
| compliance-missing | 0 | 0 | 0 |

**How we'll know it worked:** factory-speech blocking falls from 2 → near 0 in **both** replicates. CH-08 no longer tells the reader it will speak "flat" or that "we delivered the hard facts." Facts then relief still happen, unannounced. CA-SAFE title is absent from chapter body; one spoken routing clause (or silent honouring) remains. Neither book addresses the reader as Maya.

**Carr-likeness (secondary, same cause):** fear chapter reads as stacked truth then relief, not workshop stage-direction — closer to Carr's unannounced hinge.

---

## Honest limits

- **Attribution is a founder batch**, not one cause. Same as 013.
- **T-A grafting is model sampling.** W2 may not close A if Muse Spark still pastes the token. If B's announcements go to 0 and A's T-A grafts remain, blocking may be 2 vs 0 — INCONCLUSIVE on a strict both-books reading of the *count*, even if the KEEP-class announcement is gone from both. Record that as prediction risk, not a reason to skip W2.
- **Noted factory-speech will not hit 0.** Evidence-grade asides and research-brief register are untouched. This batch targets the CH-08 blocking class and the CA-SAFE box, not the whole noted floor.
- **willpower-lexicon noted** (A 51 / B 33) is Carr attacking the anti-method. Do not "fix."
- **Re-argument** is noted-only; book-arc already PASS. P2/P6/R1 are subtraction, not a new table. May not move the noted count much.
- **If the plan is not regenerated**, 013 CH-08 Voice/guardrail lines remain in the writer packet and blocking will not move.
- **PERSISTENT clock:** this is a different level (card contract + reviewer gate), not a fourth silent-execution writer sentence. Eligible.
- Belief/journey/arc already hold. This batch cannot create Carr if the model still announces; it can stop feeding the announceable operators.
