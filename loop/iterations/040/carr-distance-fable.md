CARR DISTANCE
sugar: 40%
smoking: 45%
combined: 42% (honest range 35–50; the panel cannot resolve finer than that because no lane measures resemblance)
why: The belief-change *job* is largely done — both books name the credit, run Little/Big Monster under new names (Nibbler/Sweet Con, Nipper/Smokescreen), foreclose escape routes, stage a last-ordinary-instance ritual, issue a 12-command spine, and end in "BRILLIANT! I'M FREE!" — which is why the census reads belief 13/13 + 14/14, journey 13/13 + 14/14, book-arc PASS, comparison missing 0/2. The reader *effect* is not Carr's, and the gap is in the first paragraph: GSBS opens "it will be easy. No doubt you find that hard to believe but read on. I have only good news for you," then lands WHITE DEATH, "as unnatural as drinking petrol or injecting heroin," then the biscuit question; our sugar CH-01 opens "you are about to see why there was never anything to miss," defers the promise, and sends the scare away ("we will leave the frightening medical lists where they belong, in the appendix, away from this argument") — the census `method-promise-hedge 1` in CH-01 is that sentence, and the smoking thesis "seen clearly from the start, it *can* be easy" is the same hedge unflagged. Carr's register is plain, discursive, repetitive, one man talking across a table; ours is a compressed literary stylization — "Blast never built. Blast billed. Film, thirst, second call, debate, dull head, broken sleep." and whole pages of sensory catalogue (smoking CH-14: washing line, sandpaper, toolbox, logs, gate hinge) that do no belief work; a Carr reader spots this in one paragraph, and no census class can see it because `_shared.md` rules sentence shape out of scope. Invented dialect ("packet blast", "timetable talk", "the film", "hired talk") replaces Carr's everyday words and makes the reader learn a private vocabulary instead of hearing his own. Factory residue still leaks into reader prose: "That is the whole second half of the reframe," a body-text token roll-call ("Say *the nicotine trap* as the place left… Say *It does plenty TO you*… as the one-line account"), and 22 clinician-disclaimer sentences across the two books, which Carr never puts mid-argument. The scare-then-disown, Carr's signature high-force move, is present only where a card assigns it, and the ch-1 cards assign it away. `willpower-lexicon 26/19` is mostly correct Carr usage (naming the enemy) and `factory-speech 10/5` is hygiene; neither moves the number. Smoking scores slightly higher because its CH-01 ("You lack nothing. You were caught, not weak.") and instruction spine are closer to Carr's plainness, and its research had four named personas. Net: a knowledgeable reader would say "this is a well-made Easyway-*method* book by someone else," not "this is Carr" — roughly 40–45% of the way, with the remaining distance almost entirely in register, opening force, and residue, not in argument structure.

JUDGES
keep: belief-mechanic blocking set (`harm-not-belief`, `credit-intact`, `sacrifice-standing`, `reframe-unsettled`, `reader-does-not-work`, `scare-not-disowned`, `willpower-method`) — these are Carr's actual moves and are the only lane measuring belief change. journey `journey-incomplete`, `journey-reverse`, `compliance-missing`. book-arc `journey-incomplete`, `ending-maintenance`, `mantra-system-break`. comparison `missing`/`partial` against `reference-moves` — the closest instrument to Carr-distance the panel has; keep and promote it to first-class KEEP object. voice `assigned-verdict-hedge`, `method-promise-hedge`, `shame-the-reader`, and the corrected factory-speech text ("If a Carr-shaped command carries a factory prefix, count the prefix, not the imperative"). `_shared.md` PASS test (GSBS as both texts → all 0).
change: (1) voice "Human, subject-specific speech" bullet — it names the biggest gap but has no test; give it one noted class, e.g. `literary-stylization`: fragment chains ≥3 in a row, or a paragraph of sensory catalogue with no belief verb, or invented private vocabulary used as if the reader already owns it; PASS test on GSBS must be 0. (2) `factory-speech` — add explicit examples now seen in production: naming the move in reader prose ("the reframe", "the one-line account"), body-text token roll-calls, mid-argument clinician disclaimers. (3) `method-promise-hedge` — add "it *can* be easy / does not *have to* be a struggle" to the examples; smoking CH-01 passed with exactly that hedge, so repeatability is off. (4) book-arc receives only the skeleton; give it GSBS ch-1 and ch-18 verbatim so the opening promise and ending crescendo are compared to the real thing, not to a table. (5) `_shared.md` noted band: "NOTED counts may differ by ±1 per class per chapter report" means ±13 per 13-chapter book, yet KEEP is a drop of 2 per book — either tighten declared noise or widen the band (see LOOP).
delete: (1) noted `willpower-lexicon` — Carr writes "requires discipline and perseverance" and quotes "giving up" in GSBS ch-18; the class fires on Carr, fails the PASS test, and has been a 40/31 → 26/19 distraction across eight iterations; keep only the blocking `willpower-method`. (2) `copied-mannerism` — GSBS is 100% Carr mannerism; under the Fidelity Doctrine the factory is supposed to match Carr before any twist, so a class that penalises resemblance points the loop the wrong way. (3) noted `re-argument` as a KEEP object (journey, belief, book-arc) — Carr's method *is* counter-brainwashing by repetition (GSBS ch-18 rebuilds "remove the belief that you are making a sacrifice" from scratch); 036–037 optimised against Carr's own method. Keep the class visible for `journey-stall`-type sag, never as PRIMARY. (4) `trap-question-label` — grep hygiene; fold into factory-speech. (5) `placement-miss`, `pre-debut-spend` — plan-conformance, not Carr-likeness; the chapter-reviewer owns plan conformance.

RESEARCH
The "quality ceiling" claim is true and the ceiling is currently set by a funnel, not by mining. Smoking banks total ~1,000 packets (J=130, L=180, A=50, D=100, M=40) — but `lived-experience.md` is 12 kB and `scientific-evidence.md` 13 kB, and the plan-writer reads only those two files; the writer reads only plan + card. So ~250 kB of mined voice reaches a 57k-word book through a 25 kB straw, and it shows: sugar CH-01 pastes four lowercase fragments ("treat when you feel you deserve one.") as the entire lived-voice presence. Broken: (1) Wave 1's six miners wrote into isolated worktrees, so the parent saw header-only banks for two heartbeats — a harness capability gap (sub-agent working directory), not a prompt gap, and it is not named as such in the log. (2) The prompt's own rights gate ("Reddit is excluded without explicit Reddit authorization") contradicts the North Star and AGENTS.md ("must reach forums, Reddit…"); the sacred vein is closed by the research prompt itself, and the log's community map is "non-Reddit blogs, WhyQuit, NHS/CDC quit pages." (3) Search primitives: "Bing-fallback SERPs were unusable without a brand anchor; Marginalia returned real blogs" — the harvest is one lead plus a weak search API, not 1,000+ fetches. (4) §5 floor "≥300 lived entries" was not cleared (L=180) and not documented as scarcity; §7's slot table is never shown row-by-row; synthesis proceeded on count floors, which §5 says never authorise completion. (5) Source diversity: WhyQuit "Turkey's Triumphs" and Mumsnet dominate; the >50% single-domain rule is not evidenced. (6) Nothing downstream mines the banks for *register*: Carr's plainness could be learned from the confessions themselves, but the writer never sees a raw confession. Thin, not broken: personas (four named, good), villain dossier (Dunn/Teague memos present), science (Cochrane/SG present, CONTESTED pairs thin).

LOOP
038–040 burned three paid dual runs because four rules composed into a loss function that could not fail loudly. (1) The PRIMARY-selection fallback ("empty intersection → top class of the worse subject") forced a hygiene class (`factory-speech` 10/5) that was already inside the noise band for one subject and far from belief change. (2) The voice judge's `factory-speech` included Carr-native numbered ALL-CAPS — a judge defect — and the blocking quotes (`3. BEGIN BY FEELING GREAT TO BE ESCAPING`, `10. IGNORE ANYONE WHO QUIT BY WILLPOWER`) were in the reports from 038 on, but Step 6 reads *counts*, not quotes; nobody was required to look at what the number was made of. (3) The 3-strike key is "same PRIMARY class **+ same root component**"; the hypothesizer rotated components (reviewer OVERCLAIM → reviewer HEADER → writer item 5) on the same class, so three strikes on `factory-speech` never registered as three strikes. (4) Restore triggers fired twice for "PRIMARY worsened ≥ band in both" — a hygiene class getting *worse* in both books right after a reviewer/writer change is a textbook judge-instability signal, but the rule reads it as a factory regression and re-runs. Underlying all four: the KEEP band (drop of 2 per book) is smaller than the panel's declared noise (±1 per class per chapter report ⇒ ±13 per book), so 037's 8→6 / 9→5 KEEP is indistinguishable from noise, and so were the 038–040 "misses." The rule that would have stopped it: **before any dual run whose PRIMARY is a noted or inventory class, the orchestrator runs the owning judge on the one GSBS chapter that contains the same construct (here: GSBS ch-1 with `SHELVE THE SUGAR!` / `1. FOLLOW ALL THE INSTRUCTIONS`); any count > 0 is a judge defect and halts the campaign.** Second: 3-strike counts the PRIMARY *class* alone. Third: Step 6 prints the PRIMARY class's blocking quotes in `decision.md`, and a human-readable quote that is Carr-shaped is a halt. Also record: the hypothesizer being banned from touching judges is correct, but it must be *required* to declare "judge defect suspected" as a verdict option instead of proposing a factory change — the 040-instrument learning added that rule only after the money was spent.

IMPROVEMENTS
1. [judges] Add one holistic Carr-distance judge: whole book + whole GSBS, emits 0–100 "indistinguishable-from-Carr" with three quoted reasons, PASS probe (GSBS vs itself) = 100; recorded every iteration, becomes a KEEP co-gate after three baselines. Why: it measures the thing the founder is asking me for and the panel cannot see (register, opening force, residue). Risk: subjectivity/drift — mitigate with a fixed rubric and the probe.
2. [judges] Delete noted `willpower-lexicon`, `copied-mannerism`, and `re-argument`-as-PRIMARY; fold `trap-question-label` into factory-speech. Why: they fire on Carr and have been the loop's main objects for eight iterations, pulling away from Carr's repetition and stated certainty. Risk: reviewer `RE-ARGUMENT` finding already promoted — leave it, just stop scoring it.
3. [loop] Widen the KEEP band to exceed declared judge noise: material = rate drop ≥ 25% *and* ≥ 4 at n≈13, PRIMARY baseline ≥ 12 in both; otherwise PRIMARY must be a blocking class, comparison `missing`/`partial`, or the Carr-distance score. Why: 037 and 038–040 were decided inside noise. Risk: fewer KEEPs — that is the point.
4. [loop] Instrument check before any dual run (owning judge on the matching GSBS chapter; any count → halt), 3-strike keyed on PRIMARY class alone, and `decision.md` must print PRIMARY blocking quotes. Why: would have stopped 038 before 039. Risk: one extra judge call per iteration.
5. [factory] Style guide Part B: replace the compressed/fragment prose operators with Carr's plain conversational sentence and ban sensory-catalogue paragraphs that carry no belief verb; invented private vocabulary allowed only for the two Monsters and frozen mantras. Why: register is the largest single Carr-distance and the panel has never targeted it. Risk: factory-speech may rise while the writer readjusts; acceptable under the corrected judge.
6. [factory] Chapter-1 card must assign the flat method promise in paragraph one (easy, permanent, no willpower, "you find that hard to believe — read on") and a full-force scare-then-disown; delete the plan option of routing harm to an appendix. Why: GSBS ch-1 is where Carr wins the reader's trust and both our ch-1s hedge and defer. Risk: `OVERCLAIM`/`method-promise-hedge` tension — the reviewer's OVERCLAIM bound governs facts, not the method promise; say so in the card.
7. [factory] Delete mid-chapter clinician disclaimers and CA-SAFE prose routing; one front-matter note, as Carr does. Why: 22 sentences of consent-form register in reader prose. Risk: low — safety text still exists, once.
8. [research] Fix the funnel and the gate: plan-writer reads the banks (or a ≥40 kB curated `lived-experience.md` with verbatim confessions), miners write to the parent checkout, and the founder resolves the prompt's Reddit exclusion against the North Star explicitly. Why: the writer cannot ventriloquize a voice it never hears; the current ceiling is 25 kB of synthesis. Risk: plan context size; rights exposure if Reddit is opened without a decision.

READY
verdict: yes-after
edits-first: (a) judge edits 1, 2 and the voice/`_shared.md` changes above, as one founder-guided instrument change with a fresh dual BASELINE (041 = BASELINE, no factory wording); (b) loop rules 3 and 4 in `loop/PROGRAM.md`/`hypothesizer.md` (band, instrument check, 3-strike by class, printed quotes); (c) research harness fix for miner working directory plus the Reddit decision recorded in `prompts/research-agent.md`. Items 5–7 are hypotheses for the loop to run on the new instrument, not preconditions — but 5 should be the first PRIMARY, scored on the Carr-distance judge and comparison `partial`, not on factory-speech.
