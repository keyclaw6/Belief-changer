## Failure evidence

Census class: **`factory-speech`** (voice, noted) in both 025 books: A 18 / B 16. Blocking 0/0. All lanes PASS. This is the authorized 026 anti-slop PRIMARY (open-questions item 6; Fable 026 list). ≥8 in both.

025 quotes (unassigned, noted):
- A ch10: seven ease-operator refrains ("Hear the verdict short." / "Hear the margin short.")
- A ch06: arc-room labels ("kill chapter", "inhabit chapter", "freedom warming")
- A ch08: craft aside ("Warm complicity here, never blame.")
- A ch11: numbered card-title paste before the vow

The Fable 026 import does not name those leaks. It names chatbot openers, throat-clearing, summary closers, and stacked-triplet padding — the selective anti-slop list that may register only through `factory-speech`.

## Root cause

**PRIMARY — style-guide `prompts/style-guide.md` Part B.** The writer already reads Part B as the sentence contract. Nothing in §B5 forbids chatbot framing, summary stamps, or synonym-triplet padding, so generic LLM residue is legal next to Carr operators. The 025 leaks are mostly ease-operator / card-label factory-speech; this iteration still tests the authorized anti-slop list as one bound change, not a second mechanism.

## Targeted fix

**Change 1 (PRIMARY) — `prompts/style-guide.md` — class `factory-speech` (voice) — component style-guide (§B5 operator 12 + §B9 checklist).**

Add operator 12 (chatbot residue: no throat-clearing openers, no summary closers, no stacked-triplet padding) and one matching B9 checklist bullet. No judge edit. No Not-X-It's-Y / every-always-never / question / fragment / adverb / 1–10 score import.

**Why this component:** item 6 and Fable require a selective Part B import, never a fifth writer input and never a judge class named "slop". KEEP can only register through `factory-speech`.

## Predicted impact

**PRIMARY:** voice `factory-speech` falls in BOTH books versus 025 (A 18, B 16) by the Step 6 band (drop of 2+ at same n). **Secondary, recorded not decisive:** `copied-mannerism` may fall if triplet padding was counted there; comparison `missing` 3/3 is not this iteration's object.

**Possible regressions:** the live 025 leaks are ease-operators and card titles, not "Here's the thing". If those persist, PRIMARY will not move and the verdict is REVERT — that is the KEEP object working. Do not treat a nicer-reading book as KEEP.

**How we'll know it worked:** `factory-speech` (noted + blocking) must fall by ≥2 in BOTH replicates versus 025. Chatbot-phrase grep is not evidence of KEEP.
