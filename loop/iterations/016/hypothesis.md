# Hypothesis — Iteration 016

source: founder batch — keep 015 both-books win + strengthen 015 one-book win + Carr subtractions (Grok 4.6 GO packet). Composer 2.5 evidence swarm + GSBS vs 014. Packet verify PASS.

## Failure evidence

014 KEEP. Voice blocking `factory-speech` **0 / 0**. Unpaid both-books:

- Voice noted `trap-question-label` **A 3 / B 1** — pasteable “only honest answer concedes the point” still on campaign style-guide and writer.
- Voice noted `factory-speech` **A 17 / B 38** — 015 echo-ID-only dropped B (38→13) and **raised A** (17→20) because C-14 stacked `echo M-A, M-B, M-C, M-H` and ID-only became a vow-chapter creature checklist (A ch14 noted 2→7).

015 REVERT. Closed trap-question 0/0 both. Reopened blocking (A verdict-landing from `land one short verdict`; B `8.`/`9.`/`10.` from “numbered ALL-CAPS headline”). Job-ownership raised book-arc re-argument. Evidence-ID-only did not close A’s methods register.

## Root cause

(1) Style-guide/writer still ship a speakable trap-question formula. (2) Echo-ID-only without **assignment density** turns a four-echo vow card into inventory speech. (3) Style-guide still names a population-scale historical-evidence operator and a mid-arc safe/combination coda; writer still names “land one short verdict.”

## Targeted fix

Founder batch (attribution weak). **Reverted 015 families not applied:** semantic job-ownership; instruction-headline-only + writer “numbered ALL-CAPS headline”; evidence-ID-only / W2; W3.

**Keep (both-books):** S1–S5 + W1 trap-question formula → ask; the question is the next sentence.

**Keep+alter (one-book):** echo cards cite ID only **and** ≤2 mantra IDs per card; vow/gate ≤1 echo + debut; reviewer blocks stacks. Plan regen. **15 chapters.**

**Carr extras (Grok GO, stripped):** delete `land one short verdict` (do not replace with a named stop); drop historical-evidence operator (no “millions of intelligent people” slug); expand coach-cut list on the S3 line; safe/combination planner-facing only (no writer never-speak FOLLOW ALL). Writer anatomy item 5 stays 014 `spoken imperative`.

**Plan regen:** yes. **Research:** reuse.

## Predicted impact

**KEEP conjunction vs 014:**

1. Voice noted `trap-question-label` **3 / 1 → 0 / 0** both.
2. Voice noted `factory-speech` drop **both** (A **< 17**, B **< 38**).

**Veto:** blocking `factory-speech` stay **0 / 0**. Named 015 kills (verdict-landing / instruction serials) in either book → do not KEEP. Belief / journey / book-arc stay PASS.

Confirming only: `wrong-register` 8/9, `coach-register` 5/6. Not targeted: re-argument, willpower-lexicon.
