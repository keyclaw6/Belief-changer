## Failure evidence

Journey `re-argument` is the only KEEP-eligible class in 035: **15 sugar / 16 smoking** (≥8 both; n=13/14 unchanged). Blocking 0 both. factory-speech 8/3 is not ≥8 both; willpower-lexicon is locked.

Distribution: sugar 1/1/0/0/1/2/2/2/1/1/2/2 across ch2–13 (spread); smoking 2/1/0/0/0/0/2/0/0/0/0/**7**/**4** — concentrated in the two post-vow chapters.

Smoking Ch13 journey judgment (re-argument 7): "Morning start rebuilt as full turn: 'We told ourselves the first dose cleared the head. Did it clear, or did it quiet the hollow unease…' … Tea pairing rebuilt … Shop wall rebuilt … Break-as-freedom rebuilt … Afternoon pacing rebuilt … Meal crown rebuilt … Stray-thought protocol rebuilt from C12 as its own section." Book-arc smoking: "Ch13's morning/meal/break walk largely re-inhabits Ch8's ordinary-day sections after freedom is already conferred."

Smoking Ch14 journey judgment (re-argument 4): "rebuilds the farewell-table transition as its own section … rebuilds the TO-you / FOR-you ledger as argument + turn … rebuilds the physical-hunger / creature transition settled earlier … rebuilds C13's 'when the old thought crosses' as a full section again" — "different shape and far longer than GSBS Ch. 46."

Sugar Ch8 journey judgment (re-argument 2): "'You walk past a bakery and the smell hooks…' — This opens a full section that replays CH-07's argument-evidence-turn on learned firings versus compulsion, including the fed-walker test, Sweet Con translation, and the Monday–Thursday thinning diary." Book-arc sugar Gap 3: "Same structure, same week-long fade proof, new heading." Sugar Ch12 (re-argument 2): "A full argument-evidence-turn shop sequence rebuilds the single-paragraph shop preview already settled in the previous chapter."

Book-arc named restages: sugar box-at-ten (ch2→4), ditch-rescuer (ch3/5→7), cue-bell (ch7→8), tomorrow-delay (ch10→11); smoking heaven-at-lighting (Ch2→3), party/pellet (Ch3→5), mild theatre (Ch5→9), Heaven-for-Three-Puffs (Ch10), morning/meal/break (Ch8→13).

## Root cause

The trace analyzer names Cluster 2 as eligible but assigns no root component this iteration. The A1 reviewer traces supply one. Every named restage chapter received a reviewer `LENGTHEN` demand and then roughly doubled:

- smoking ch13 review-01: "LENGTHEN to B±15% — Delivered 1845 words. Budget 4500 … Add ~2000+ words that serve the card's primary job" → final 4660 words, journey re-argument 7. The card itself said "scene: token-echo SC-A as patio phrase only; token-echo SC-B as morning phrase only … originality: live days, do not restage settled scenes." Rounds 2–3 fired only `UNASSIGNED-REFRAIN` and `HEADER`; the seven rebuilt sections passed unremarked.
- smoking ch14 review-01: "Delivered 2052 words vs Budget 5000 … Add ~2200-3700 words" → 4346 words, re-argument 4, ACCEPT on round 3.
- smoking ch3: 1659 → 4578 ("Add ~2200+ words"); sugar ch8: 2111 → 4633 ("Add words serving the card job"); sugar ch4: 3080 → 4230; sugar ch11: 2465 → 3172. All book-arc-named restages.

Mechanism: the rewrite handoff tells the writer "Obey LENGTHEN/SHORTEN … do not add new jobs; do not continue into a reserved-later job," and the reviewer fences forward with `RESERVED-REACH`. Nothing fences backward. The only legal reservoir for +1000–2700 words is material earlier cards already settled, so Spark rebuilds it as fresh titled sections (argument + evidence + turn — the exact census definition). `prompts/chapter-reviewer.md` lists JOB/MANTRA/INSTRUCTION/ID/LENGTH/HEADER/STOPPED-SHORT/UNASSIGNED-REFRAIN/RESERVED-REACH/OVERCLAIM and states "No other finding types," so a padded rewrite is `ACCEPT`. Style-guide §B7 promises "The reviewer rejects a chapter that: … re-argues settled material" — no finding implements it. The reviewer has what it needs to detect this without the previous chapter: the full plan (scene-bank debut cards, each card's belief-now/primary job, instruction owning chapters).

Corroboration across iterations: journey re-argument was 7/5 at 019 (no reviewer), then 12/10 at 025 the moment the length-enforcing reviewer arrived (under a *tighter* re-argument definition after the founder halt), 16/8 at 033, 15/16 at 035. 020–024 attacked plan-card fields and writer-prompt sentences five times with no both-books movement — the amplifier was not in those components.

## Targeted fix

**Change 1 (PRIMARY)** — `prompts/chapter-reviewer.md` — journey `re-argument` — chapter-reviewer finding list + ACCEPT gate — both subjects.

Insert after the `RESERVED-REACH` bullet:

```
- `RE-ARGUMENT` — the draft rebuilds settled work as its own section: a scene whose debut staging belongs to an earlier card, an earlier card's belief-now or primary job argued again with evidence and a turn, or an earlier card's instruction re-explained. Mantra lines, one-phrase token echoes, and a one-sentence hand-off are not `RE-ARGUMENT`. Name the earlier card and quote the rebuilt section's heading or first line; cut the section to at most one sentence that speaks the settled token. If that cut leaves the chapter below 0.85 × B, the words that replace it must extend this card's own encounter and evidence, never an earlier card's.
```

Replace the ACCEPT sentence:

```
`ACCEPT` only when every check above is fine (length inside ±15% of B, job
done and stopped and landed, assigned mantras/instructions verbatim, IDs
resolved, no `HEADER`, no unassigned refrain, no reserved-later job, no
overclaim).
```

with:

```
`ACCEPT` only when every check above is fine (length inside ±15% of B, job
done and stopped and landed, assigned mantras/instructions verbatim, IDs
resolved, no `HEADER`, no unassigned refrain, no reserved-later job, no
re-argument, no overclaim).
```

This supersedes the unimplemented style-guide §B7 promise ("The reviewer rejects a chapter that: … re-argues settled material") and the unenforced card guardrail "do not restage settled scenes." No other file changes. Style-guide §B5 operator 9 untouched. No plan edits, no model/route changes. Not a replay of 020–024 (plan-skill continuity/room-name/one-correction; writer previous-chapter/craft 3), 028/029 (style-guide headers), 030 (HEADER), or 032 (writer anatomy). No prior iteration has given the reviewer a backward fence.

**Why this component:** The reviewer's LENGTHEN loop is the only factory component whose traces show the re-argument being manufactured (draft → +2000 words → rebuilt settled sections → ACCEPT), and it is the one component with a forward fence (`RESERVED-REACH`) but no backward one — 020–024 already exhausted plan-card and writer-prompt sentences with no both-books effect.

## Predicted impact

PRIMARY: journey `re-argument` falls in BOTH replicates by ≥2 at unchanged n — sugar 15 → ≤13, smoking 16 → ≤14. Smoking Ch13/Ch14 (7/4) are the largest predicted movers; sugar ch7/8/12/13 pairs next.

Secondary (recorded, not scored): book-arc `re-argument` 4/5 predicted to fall; belief `re-argument` 8/8 predicted to fall.

What might regress: words fall (restaged sections cut) — must stay ≥48,000; current 55,356/56,649 gives ~7,000 words of margin, and the finding routes replacement words to the card's own encounter. A1 CAP count may rise (one more finding type to clear within K=3). `journey-stall` 5/4 could rise if Spark fills length with same-rhythm scene blocks instead of restages. factory-speech (8/3) is non-regression only, not a veto; blocking must stay 0 both.

How we'll know: journey `re-argument` census count falls ≥2 in BOTH replicates vs 035 (15/16). Reviewer traces firing `RE-ARGUMENT` without the census drop is not KEEP. One book falling while the other holds or rises is QUANTIFY.
