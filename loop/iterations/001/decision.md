# Decision — Iteration 001 (scaffold firewall)

**Verdict:** REVERT

**Predicted:** Clusters 1 (evidence-grading scaffold) and 2 (factory taxonomy / craft directives) close in the voice lane because the writer-prompt no longer commands literal preservation of ledger vocabulary.

**Observed:** Both targeted clusters **partially closed, not closed**. Leak log and judges still quote `P-0x`, `Killer line`, `your card assigns`, `argue-to-compress beat`, and one `CONTESTED` (C08). Voice-emotion remains 4/18 PASS, 14 FAIL (same count as 000; passing chapters differ — sampling-sensitive, and the judge model changed). Research-report register remains (audit effect without grade labels). Book-arc FAIL (000 was PASS on a different judge model). C13 FAIL on all three chapter lanes (Nature's Guide as toolkit).

**Prediction accuracy:** partial. The preserve-every-grade clause was the right *surface* to cut — verbatim `SUPPORTED`/`Permitted inference` dumps largely vanished — but the same writer-prompt still *speaks* craft and card diction, so Cluster 2's class survives, and Cluster 1 mutated into epidemiological briefing.

**Owning lane:** voice-emotion. Targeted clusters did not close. Other-lane notes (belief C13; book-arc FAIL; instruction-paperwork) are not used as KEEP vetoes because KEEP is already unavailable; instrument confound vs 000 DeepSeek is recorded in `trace-analysis.md` and must not be read as a clean regression score.

**Why not KEEP:** material improvement requires the predicted class to disappear from the book-wide failure set. Residual factory scaffolding in reader prose is still the voice lane's dominant defect.

**Why not INCONCLUSIVE:** evidence is complete (54/54 + book-arc + trace). Partial close is a real finding, not missing data.

**Factory change:** not promoted. `prompts/chapter-writer.md` firewall remains on `campaign-001` as the founder-applied pre-001 edit (`be106a5`); this iteration does not add a further accepted state. The 001 book is discarded with the worktree.

**Next:** hypothesizer — one causal change that closes residual factory scaffolding (same component, different instruction: Binding chapter craft still emits speakable vocabulary). Do not repeat the evidence-honesty clause swap.
