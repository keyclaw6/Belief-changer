source: hypothesizer
change_status: applied in worktree iter-002

## Failure evidence
Cluster 4 — Cross-chapter re-argument and mid-book curve stasis — systemic/positional mid C10–C14, book-arc FAIL. book-arc Gap 3: C10 re-walks all four benefits *“Energy? We reassigned it… What is left in FOR? Nothing”* after C07 closed the ledger; Gap 4: C12 rebuilds the hundred-cord appliance S-08 C08 already completed; Gap 5: teeth/heart/inflammation scare package at full force in C03, C10, C12, C13 and again as fresh bullets in C14 readiness audit; reader C02-G1: confidence-trick S-04 rebuilt from scratch after C01 landed it; reader C02-G2: E-11 14%/12% prevalence block rebuilt; voice C12-G1 secondary meta at scare turn. Effect: demolition curve peaks early, C10–C14 that should compress spend length re-proving; book-arc regressed PASS(000)→FAIL(001).

## Root cause
Plan — via its generator. Trace §4 Evidence: plan.md §7 cards duplicate artifacts without echo-only guard — C01 and C02 both carry `S-04 confidence-trick` + `E-11`; C08 assigns `S-08 (100 cords)` and C12 assigns `S-08 variant` full re-stage; C10 card assigns `devices: awe catalogue, "doing TO you" ledger` after C04–C07 demolished benefits; C14 recap list restates E-01–E-19 including health scares. Curve map says demolition *“hands off to freedom vocabulary after C13”* but cards violate it. The factory file responsible is `prompts/master-plan-skill-v2.md`: its canonical repetition law lives only in `### Mantra and frozen-token sheet` as *“Chapter cards reference mantra IDs and frozen-token IDs and never repeat the frozen wording.”* — scoped to mantras/frozen tokens only. `### Scene and analogy bank` (*“never copy the full scene… into the card”*) and `### Compact evidence ledger` only forbid copying full text, not re-assigning the same S-xx/E-xx ID for full re-argument in a later card. `### Arc and length` *“build cumulatively rather than repeat a plan-wide inventory”* is unenforceable without a debut-once rule. Writers therefore faithfully rebuild from card inputs. The required move (invoke settled work by token) is wrong in the skill before the plan runs. Checked `loop/learnings.md`: iter-001 was a writer-prompt evidence-honesty clause swap (reverted) — different component and different mechanism; this targets the planner's debut-once contract, not ledger vocabulary.

## Targeted fix
File: `prompts/master-plan-skill-v2.md` — one causal change: replace the narrow mantra-only repetition law with a debut-once, echo-by-token law that governs mantras, scenes, and evidence rows.

In `### Mantra and frozen-token sheet` replace the canonical sentence:

`Chapter cards reference mantra IDs and frozen-token IDs and never repeat the frozen wording.`

with:

`Chapter cards reference mantra IDs and frozen-token IDs and never repeat the frozen wording. Repetition law (debut-once, echo-by-token): mantras, frozen tokens, scenes/analogies (S-xx), and evidence-ledger rows (E-xx/LEU/SEU) are debut-once, echo-by-token thereafter. After a scene or evidence row's debut chapter, every later chapter card may invoke it only by its short token/name in ≤1 sentence (no rebuilt scene, no re-presented prevalence/stat block, no re-staged appliance/metaphor, no re-argued benefit ledger). Licensed recap zones remain only IN THIS CHAPTER previews, SUMMARY bullets, and the instruction assigned to that chapter. No later card may re-assign the same S-xx or E-xx for full re-argument — echo only.`

This canonical law now governs the duplicate implications in `### Scene and analogy bank` and `### Compact evidence ledger` (which already forbid copying full text); no second behavior is changed — later cards must reference prior S-xx/E-xx by token, not re-debut them.

**Why this component:** The book-arc judges fail because the planner re-assigns the same scenes/evidence to multiple cards with no invoke-by-token contract; fixing writer-prompt diction cannot stop the planner from ordering rebuilds, and frozen-instruction wording (Cluster 3) is lower priority per priority ordering — arc break outranks voice effect.

## Predicted impact
What will improve: Cluster 4 will close because the planner will no longer generate cards that assign S-04+E-11 to both C01 and C02, S-08 to both C08 and C12, awe-catalogue ledger to C10, or health-scare bullets to C14 — later writers will receive only token-echo inputs, so C10 will name the ledger verdict, C12 will name 100 cords, and C14 will compound conviction rather than re-list scares, restoring compression after C13.
What might regress: This could weaken explicit recall for skimming readers because token-only echoes are briefer than rebuilt proofs — if too terse, belief-mechanic judges could note under-supported invocation.
How we'll know it worked: The owning book-arc judge should now PASS Gaps 3/4/5 (no re-walk language, no 100-cord re-stage, no repeated scare bullets) and reader C02/C13 transition should no longer note circular rebuild; voice FAIL count may remain but book-arc lane returns to PASS.
