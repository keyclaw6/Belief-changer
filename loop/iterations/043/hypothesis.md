## Failure evidence

Lane `comparison`, both subjects — KEEP-eligible intersection (missing + partial in both; deficit 24/32 also present but smoking +4 is trace-ruled noise, not the target).

Census 042: quit-sugar `missing` 6 / `partial` 8; quit-smoking `missing` 2 / `partial` 4. Blocking 0 both. No restore.

Trace-analysis cluster 2 (plan, PERSISTENT, sugar-only manifestation of a both-subjects class — signal for 043 because the class is present in both):
- sugar ch04 missing G08-M1/M2: "ch04 answers the fuel anecdote with no target/scales argument"
- sugar ch05 missing G12-M1/M2 + partial G12-M3: "ch05 shows roast-potato enjoyment but never teaches the senses-trust rule or grants a margin"
- sugar ch09 missing G06-M2 + partial G06-M1/M3: "ch09 names design-profit without the machine/oil-light turn"
- sugar ch10 missing G15-M1 + partial G15-M2: "ch10 lands company-pleasure with no selfish order and no nature-foods prong"
- plus ch01/02/03/13 partial halves both subjects.
- smoking missing 2 / partial 4 flat on the same regenerated-plan mechanism — one-subject side is noise, but the class intersection (missing in both, partial in both) is eligible.

Not deficit: 042 Reason 3 clinician kill verified both (grep 12/13→1/13 sugar, 9/14→0/15 smoking; factory-speech 6→0 / 9→1 with it). Smoking deficit 28→32 is opposite-direction on the identical change with catalogue/header residuals the hypothesis didn't touch — trace root `model`, explicitly not a factory regression, not the next target.

## Root cause

Root component: plan (`prompts/master-plan-skill-v2.md` + accepting reviewer), per 042 trace evidence.

The writer, bound to "complete exactly the assigned card job," correctly executes CH-04 fuel-inversion-only (no weight job), CH-05 reward/love/identity (no senses-trust or margin job), CH-09 engineered desire + testimony (no body-machine job), CH-10 strongest-case + myths + pity (no selfish order) — so unassigned GSBS moves stay absent. Research reuse means material was available; the drop appears exactly at the regenerated cards.

Causal instruction:
1. `prompts/master-plan-skill-v2.md`, Arc and length: `Use your judgment to merge, reshape, move, or omit material that cannot meet this boundary honestly and compellingly.` — licenses the planner to drop justifications on regeneration with no coverage obligation. PERSISTENT plan/plan-card root 7+ times (000, 002, 003, 004, 020, 023, 024); card re-cuts keep dropping unassigned moves.
2. `prompts/master-plan-reviewer-v2.md`, Whole-book cumulative walk, blocks repeat/re-argue/restage/handoff breaks but never blocks an unowned justification — so an omission plan still returns `fit to write from`.

## Targeted fix

**Change 1 (PRIMARY)** — `prompts/master-plan-skill-v2.md` — class `comparison` `missing` — component: Arc and length, merge/omit license — both subjects. Replace:

> Use your judgment to merge, reshape, move, or omit material that cannot meet this boundary honestly and compellingly.

with:

> Every reader justification in the lived-experience synthesis must be owned by exactly one argument-bearing card as that card's demolished objection, in that card's distinct concrete encounter; no justification is catalogued for later, merged away, or left unowned, and no two argument-bearing cards share the same principal encounter. Merge, reshape, or omit only material that has no owning justification after this coverage is met.

**Change 2** — `prompts/master-plan-reviewer-v2.md` — class `comparison` `partial` — component: Whole-book cumulative walk — both subjects. Replace:

> Every later argument-bearing card must build from the preceding belief now. Block adjacent cards that repeat the same principal encounter, re-argue settled work, restage a debuted scene's full job (later cards token-echo only), break a handoff, or reserve work backward.

with:

> Every later argument-bearing card must build from the preceding belief now. Block adjacent cards that repeat the same principal encounter, re-argue settled work, restage a debuted scene's full job (later cards token-echo only), break a handoff, or reserve work backward. Block any reader justification from the lived-experience synthesis that has no single owning demolition card with its own distinct concrete encounter, and block any argument-bearing card whose primary job names no owned justification demolished in its own encounter.

**Why this component:** the judge quotes absent encounters, and the planner originates card job boundaries while the writer correctly executes only the assigned job — a writer or reviewer-only fix cannot create unassigned moves; the planner law creates the owner and the reviewer gate prevents accepting an omission plan.

Retry status: not a replay. None of 020 (continuity), 021 (previous-chapter), 022 (craft 3), 023 (one-correction), 024 (room-name), 028/029 (style-guide header), 030 (HEADER), 036 (RE-ARGUMENT), 037 (LENGTHEN), 038 (OVERCLAIM), 039/040 (HEADER ownership / writer anatomy item 5) touched this merge/omit license or this coverage BLOCK. 027/031 edited `production-books/.../master-plan.md` Job tails directly (evidence, forbidden as hypothesis) — this is a regeneration-surviving factory law, not a card re-cut. Pre-spend GSBS control: G08 weight, G12 senses-trust/margin, G06 machine, G15 selfish order are all PRESENT in GSBS — the control does not fire; this is factory omission, not judge defect.

## Predicted impact

PRIMARY: comparison `missing` falls in BOTH subjects vs 042 (sugar 6→≤4, smoking 2→0). Secondary (recorded, not decisive): comparison `partial` falls in both (sugar 8→≤6, smoking 4→≤2) as halves gain owners; Carr-distance deficit flat-to-noise (sugar residual private-vocab/catalogue, smoking catalogue/header is model-level and explicitly not chased).

What might regress: forcing every justification to own a distinct-encounter card could stretch budgets or force a thin demolition to meet coverage — LENGTHEN/word-budget and reviewer ACCEPT still govern; if coverage forces an extra card, length arithmetic must still sum 54,000–66,000.

How we'll know it worked: PRIMARY `missing` count must fall in BOTH replicates, and no 043 comparison report may score MISSING on a justification with no owning card encounter.

```
parent: 13ad2af2
instrument: 2026-09-06 Carr-distance panel
```
