# Iteration-1 judgment — decision

## Outcome

Both remediated books strictly dominate their rejected bases with no observed
regressions:

- Sugar (A=remediated/B=base in AB): remediated wins progression and economy
  in BOTH orders; voice flips to remediated only in BA (weak, single-order).
- Smoking (A=base/B=remediated in AB): remediated wins argument in BOTH
  orders; emotional_movement flips to remediated only in BA (weak). The same
  OVERCLAIM critical ("relapse is common") tracks the BASE book in both
  orders. No pro-base win in any dimension or order.

Grounding: 50/50 quoted passages resolve by code against attributed texts.
Variance is low; only minor single-order flips. Full machine record (protocol,
label map, four judge outputs, per-dimension tables) is retained in
`loop/history/iter1-judgment.json`.

## Single Iteration-2 learning (iter1-learning-01)

Extend evidence-mapping discipline to ALL generated wrapper prose
(brief-derived front matter, plan-derived chapter headers, chapter
opening/closing transitions): every empirical-sounding sentence outside
claim-mapped body prose must carry an explicit evidence citation resolvable
in the dossier or be rephrased to non-empirical language before assembly.
Basis: both final audits' material findings concentrated in wrappers, never
in evidence-bound chapter bodies. Implemented as the deterministic advisory
wrapper screen appended to assembly screening for mandatory auditor triage,
plus performer discipline in Iteration-2 drafting.
