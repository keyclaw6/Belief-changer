# Hypothesis — Iteration 015

source: founder batch (explicit override of one-causal-change; hypothesizer + Carr whole-book + critic swarm)

## Failure evidence

014 KEEP closed factory-speech **blocking** 2→0 / 2→0. Voice/belief/journey 15/15, book-arc PASS both. Unpaid:

- Voice noted **factory-speech** A **17** / B **38**: evidence-grade register (SUPPORTED limits pasted as methods-section prose), frozen-token backtick/echo inventory, trap-question stage-direction (`Ask the trap questions whose only honest answer concedes the point`), I-04 safe-mechanics coda.
- Book-arc noted **re-argument** A **5** / B **4**: cinema/treat-kill restaged under a new scene ID (CH-04 → CH-12), frog slow-heat rebuilt, rescuer cited before debut. 014 bound scene-**ID** debut-once; the mutation is **job** restage.

Not KEEP: A-only CH-01 we-will; B-only CH-09 job recap; noted willpower-lexicon (Carr attacking the anti-method). Do not re-run 014 inhabit/world.

## Root cause

Same hydra one layer down from 014 Voice-operator strip. Cards still paste ledger workshop, backtick echo inventories, and a second frozen slogan; style-guide §9 still hands writers a pasteable trap-question formula. Scene-bank law still treats job as a scene-label and restage as adjacent-only, so CH-12 can debut a new SC-ID for a demolition CH-04 already made true.

Founder-authorized simultaneous rewrite of writer, plan-skill, plan-reviewer, and style-guide. KEEP attribution to one cause will be weak.

## Targeted fix

**Files:** `prompts/chapter-writer.md`, `prompts/master-plan-skill-v2.md`, `prompts/master-plan-reviewer-v2.md`, `prompts/style-guide.md` (see `change.diff`).

Seed subtraction (014 Voice-operator move, next seeds): cards cite evidence IDs only; echo cites ID only in plain text; instruction frozen wording is the ALL-CAPS headline only; trap-question formula deleted (`ask` / the question is the next sentence — no do-not-prefix). DROP W3 (sixth-strike never-paste).

Semantic job-ownership: the job that debuts is what the chapter **makes true**; a later card must not make that demolition true again under a new encounter or a new scene ID; cards must not cite a scene ID before its debut; reviewer blocks a **later** card (not only adjacent).

Critic swarm then two Grok diff-passes: KEEP/strike GO; Carr remaining hunks applied; second pass **GO** all three. CA-01 stays boxed. Willpower Method kept. Judges/PROGRAM/models/`research-agent.md` untouched. IN THIS CHAPTER heading kept.

Planning rerun required. Research reused.

## Predicted impact

**What will improve:** voice noted `factory-speech` in **both** books (014: 17 / 38) **and** book-arc noted `re-argument` in **both** (014: 5 / 4). Regenerated plan: no SUPPORTED/permitted paste on cards; echo cards ID-only; CH-12 does not re-own CH-04's treat-kill under a new SC-ID.

**What might regress:** belief/arc if job-ownership over-prunes a needed later encounter; factory-speech hydra if the writer still invents methods-register without card paste; attribution weak (batch).

**How we'll know it worked:** noted factory-speech drops in **both** vs 014 **and** book-arc noted re-argument drops in **both**. Blocking factory-speech stays 0. Belief/journey/book-arc remain PASS. KEEP attribution: founder batch, not one cause.
