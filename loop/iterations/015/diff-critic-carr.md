# Diff-critic-carr — APPLIED 015 (critic 2)

Read: `git diff -- prompts/`; `whole-book-reader.md`; `critic-carr.md`; `critic-consensus.md`; live `prompts/master-plan-skill-v2.md` + `master-plan-reviewer-v2.md`; 014 `production-books/quit-sugar/master-plan.md` cards (job assignment, not prose). No prompt edits in this file. No GSBS sentences.

Question: do APPLIED **P4 rewrite + P-job + P-scene + R-job** bind semantic job restage (cinema under a new SC-ID, frog twice, rescuer rebuild) without naming those pictures in `prompts/` and without cloning twenty GSBS rooms?

---

## Carr-test of the four hunks as applied

No cinema / frog / sofa strings in the 015 `prompts/` diff. No extra GSBS rooms. No inhabit/world re-run in these hunks. No writer restage sentence. Those MUST-NOTs hold.

The KEEP object still fails. 014 already bound **scene-ID** debut-once / token-echo. The mutation is **job** restage. The applied nouns are `argumentative job` / `new scene ID` / `Block adjacent cards`. Held against the live 014 plan, that is still ID-adjacent law with a new label.

| Named restage (014 plan, not pictures in prompts) | Bound? | Why |
|---|---|---|
| **Cinema under a new SC-ID** | **No.** | CH-04 already *makes true* treat/love credit-reassignment (sofa encounter, SC-08). CH-12 *debuts* SC-02 for the same correction under a new ID, labeled strongest-case. P-job sits in the scene-bank section, so “argumentative job” reads as that row’s job string: SC-08 ≠ SC-02, so minting SC-02 is legal. P4’s restage tail is the **adjacent-cards** sentence — CH-04 and CH-12 are not adjacent. R-job tacks the same clause onto the same adjacent list. CH-11’s reserved-later line even *assigns* the restage to CH-12; P4’s fence would shame that one line and still leave CH-12 free to debut SC-02 as a new job. |
| **Frog twice** | **No.** | CH-03 debuts SC-06. CH-08 and CH-09 already cite it **token-echo only**. Live restage is the canyon/dentist *encounter* doing slow-heat again, or the writer rebuilding a legal echo — not a new SC-ID. Applied law only bans re-own “under a new scene ID.” Critic-carr’s P4 grain was “new encounter or SC-ID.” Applied P4 dropped **encounter**. |
| **Rescuer rebuild** | **No.** | Same ID (SC-09): CH-05 “token echo” *before* debut, CH-07 “debut full staging.” P-job forbids a *later* card re-owning under a *new* scene ID. Preview-then-debut is two stagings of one ID. 014’s “later cards” echo law already implied this; the plan still assigned it. Applied text does not say cards before debut must not cite the ID. |

014 `re-argue settled work` was already on that adjacent list. CH-12 still shipped. Adding “under a new scene ID” to the same list does not make the reviewer compare CH-12 to CH-04.

Factory-speech hunks (P1–P3, P5–P9, R1–R3, W1/W2/W4, S1–S8) are a different KEEP object. This critic does not re-litigate them. They do not bind restage.

---

## What already holds (do not re-issue)

- No named cinema / frog / sofa in these four hunks. Keep it that way.
- No 20-room clone. No second inhabit chapter. No Nature’s Guide / Incredible Machine as extra rooms.
- 014 inhabit-as-primary, first-third encounters, ITC-as-rooms, post-vow second-manual block — already live. Do not add inhabit/world SEARCH/REPLACE.
- No writer “do not restage” / “perform restage silently.” Do not add one. Writer rebuild of a card that already says token-echo is not this remaining list.
- No new audit table. No GSBS prose.

---

## Remaining SEARCH/REPLACE (job-ownership only)

Unique in the named file as of APPLIED 015. Factory-speech clauses in P4 stay as applied. Do not name subject pictures.

### P4 — `prompts/master-plan-skill-v2.md`

The reserved-later fence sentence is fine. The restage tail must leave the adjacent-encounters rule and bind **later** cards to what a prior card **made true**, under a new encounter or a new scene ID.

SEARCH:
```
For every argument-bearing card, make explicit: the belief now (what is true for the reader at entry, and what this chapter makes true); the concrete subject-specific encounter; evidence-ledger IDs only; any NEW instruction (spoken ALL-CAPS headline only); and the reserved-later fence (work assigned only to named later chapter cards — a reserved job is not a later restage of a debuted demolition). The next argument-bearing card enters from the belief now just installed. Adjacent cards must use distinct encounters and build cumulatively rather than repeat a plan-wide inventory or restage a debuted argumentative job under a new scene ID.
```

REPLACE:
```
For every argument-bearing card, make explicit: the belief now (what is true for the reader at entry, and what this chapter makes true); the concrete subject-specific encounter; evidence-ledger IDs only; any NEW instruction (spoken ALL-CAPS headline only); and the reserved-later fence (work assigned only to named later chapter cards — a reserved job is not a later restage of a debuted demolition). The next argument-bearing card enters from the belief now just installed. Adjacent cards must use distinct encounters and build cumulatively rather than repeat a plan-wide inventory. The job that debuts is what this chapter makes true. A later card must not make that same demolition true again under a new encounter or a new scene ID; it may name the already-true picture in one breath.
```

### P-job — `prompts/master-plan-skill-v2.md`

Grain: job = demolition the owning card makes true, not a new scene’s label. Same-ID preview-then-debut is two stagings. Two IDs must not carry one demolition.

SEARCH:
```
Chapter cards reference scene/analogy IDs. A scene debuts its full staging on exactly one card; later cards may cite that ID only as a one-phrase token echo, never a second staging or restated full job. An argumentative job debuts on exactly one card; a later card must not re-own that job under a new scene ID. They never copy the full scene or analogy into the card.
```

REPLACE:
```
Chapter cards reference scene/analogy IDs. A scene debuts its full staging on exactly one card; cards before that debut do not cite the ID; later cards may cite that ID only as a one-phrase token echo, never a second staging or restated full job. An argumentative job is the demolition the owning card makes true, not a new scene's label. It debuts on exactly one card; a later card must not re-own it under a new encounter or a new scene ID. Two scene IDs must not carry the same demolition. They never copy the full scene or analogy into the card.
```

### P-scene — `prompts/master-plan-skill-v2.md`

Compact field must match: new SC-ID **or** new encounter cannot carry a demolition already made true.

SEARCH:
```
- one or more concrete scene/analogy IDs: debut card carries the staging job; later cards that cite the ID mark it as token-echo only — never a new scene ID whose job is a settled demolition;
```

REPLACE:
```
- one or more concrete scene/analogy IDs: debut card carries the staging job; later cards that cite the ID mark it as token-echo only — never a new scene ID or a new encounter whose work is a demolition a prior card already made true;
```

### R-job — `prompts/master-plan-reviewer-v2.md`

Take job restage off the adjacent list. Whole-book walk already tracks belief now; the blocker must be **a later card**, or CH-12 vs CH-04 stays invisible. Keep the post-vow second-manual sentence untouched. Do not add inhabit/world.

SEARCH:
```
Block adjacent cards that repeat the same principal encounter, re-argue
settled work, restage a debuted scene's full job (later cards token-echo
only), restage a settled argumentative job under a new scene ID, break a
handoff, or reserve work backward. After the vow, block a
second manual: no restaged settled scenes, no new thought-curriculum.
```

REPLACE:
```
Block adjacent cards that repeat the same principal encounter, re-argue
settled work, restage a debuted scene's full job (later cards token-echo
only), break a handoff, or reserve work backward. Block a later card that
re-owns a demolition a prior card already made true, under a new encounter
or a new scene ID. After the vow, block a
second manual: no restaged settled scenes, no new thought-curriculum.
```

Orchestrator (not a hunk): regenerate the 15-chapter plan after this remaining apply, or 014 cards still debut the treat-kill again under a new SC-ID, echo the rescuer before its debut, and pair slow-heat encounters with a token that the writer then rebuilds.

---

## MUST-NOT

- Name cinema, frog, sofa, birthday, or other subject pictures in `prompts/`.
- Clone 20 GSBS rooms; add inhabit/world re-run; add a writer restage sentence.
- Reopen CA-01; ban or rename the Willpower Method; kill IN THIS CHAPTER; edit judges / PROGRAM / models / `research-agent.md`.
- Paste GSBS prose; new re-argument audit table; edit `production-books/` as the hypothesis.

---

## Verdict

APPLIED P4 + P-job + P-scene + R-job are the right files and the right *shape* (plan job, not writer never-announce). They do not bind the three restages: “argumentative job” is still the scene-bank label, restage is still “new scene ID” not encounter, R-job is still adjacent. 014 CH-12 would still debut a second treat-kill.

**NO-GO**
