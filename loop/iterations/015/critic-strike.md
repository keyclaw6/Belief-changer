# Critic 3 of 3 — 015 sixth-strike / mechanism

Clock (census instrument, factory-speech at prompt/card): 010 writer silent craft → 011 style-guide silent operators → 012 residual → 013 packet hygiene → 014 card Voice-operator strip. Learnings name that residual **5th time**. Another never-announce / never-paste / perform-silently **writer** sentence is strike six. 014’s eligible move was seed subtraction: stop putting a speakable formula on the card (or in the style-guide clause the planner copies), so the writer has nothing to paste. Same move, next seeds = ELIGIBLE. Same exhausted writer/checklist ban = DROP.

Test per hunk: **(a)** seed subtraction of a speakable formula / card paste, same move as 014 Voice-operator strip — **ELIGIBLE**. **(b)** another “Never announce / never paste / perform silently” writer sentence — **STRIKE SIX, DROP**.

`target-choice.md` already named the tell: if the hypothesizer writes “Do not prefix trap questions,” that is strike six.

---

## Per hunk

### `prompts/master-plan-skill-v2.md`

| ID | Verdict | Why |
|---|---|---|
| **P1** | **(a) ELIGIBLE** | Planner contract: cards cite evidence IDs only; never paste permitted/prohibited/SUPPORTED/must-state onto the card. Same move as 014 P3/P8 (cite ID, don’t paste workshop). Not a writer never-announce. |
| **P2** | **(a) ELIGIBLE** | Strip backtick wrap + echo-card quote inventory. Debut pin stays once, plain text. Seed of frozen-token paste is the card, not a writer ban. 014 Voice-operator analog. |
| **P3** | **(a) ELIGIBLE** | Subtract the frozen second slogan from the spine row (`plus at most one short spoken rationale` → headline only). I-04 “miss one number / safe stays locked” is plan paste, same class as 014 boxed-title paste. Numbered ALL-CAPS headlines stay. CA-01 unsplit. |
| **P4** | **(a) ELIGIBLE** for IDs-only; **DROP the restage tack-on** | Body: `evidence IDs plus the limits the writer must not overclaim` → `evidence-ledger IDs only`. That is P1’s seed. The added `or restage a debuted evidence job` is **not** 014 Voice-operator strip and is **not** a new mechanism (see below). Apply P4 without those six words. |
| **P5** | **(a) ELIGIBLE** | Compact-card duplicate of P4’s evidence field. Limits stay in the ledger. No restage clause. |
| **P6** | **(a) ELIGIBLE** | Compact-card duplicate of P2. Debut pin plain; echo ID only. |
| **P7** | **(a) ELIGIBLE** | Extends 014 P7’s card do-not-put: no device name, no “use SUPPORTED limits” as a card instruction. Planner-facing. |
| **P8** | **(a) ELIGIBLE** | Same 014 P8 list, next seeds (evidence workshop, backticks, trap-question device names). “A card is a room…” is planner metaphor, not a writer silent-execution sentence. |
| **P9** | **(a) ELIGIBLE** | Normalization of the same strip. Debut plain-text pin remains the exception. |

### `prompts/master-plan-reviewer-v2.md`

| ID | Verdict | Why |
|---|---|---|
| **R1** | **(a) ELIGIBLE** | Stops requiring the paste P1/P4 subtract. Same move as 014 R3 (absence of Voice: is not a blocker). |
| **R2** | **(a) ELIGIBLE** | Blocks the seeds this batch strips if they reappear. 014 R2 pattern. |
| **R3** | **(a) ELIGIBLE** | Absence of limits-on-card / echo pins is not a blocker. Without this, R1’s old “limits the writer must not overclaim” requirement returns. |

### `prompts/chapter-writer.md`

| ID | Verdict | Why |
|---|---|---|
| **W1** | **(b) STRIKE SIX, DROP** as written | SEARCH correctly deletes the pasteable clause (`questions whose only honest answer concedes the point`). REPLACE puts `(the questions are the next sentences, unprefixed)` in its place and keeps `Perform every rhetorical move silently`. “Unprefixed” **is** the named tell (`Do not prefix trap questions`). Formula deletion alone would be (a); this hunk as written is another perform-silently writer sentence. |
| **W2** | **(a) ELIGIBLE** | Dominant change: delete the writer-prompt seed `Internally hold every evidence grade… permitted inference, prohibited inference…` (010 leftover that still names the methods vocabulary) and point the writer at card IDs → ledger. Same move as 014 supporting writer hunk matching the card strip. `Never narrate study design, grades, or methods` already exists; adding `permitted/prohibited cells` is ban-list mention of P1’s stripped cells, not a new silent-execution program. Do not treat that add as the mechanism. |
| **W3** | **(b) STRIKE SIX, DROP** | Pure additive writer never-paste: `never markdown backticks around the token, never a consecutive inventory of the same slug`. Writer already bans backticks around craft. The seed is P2/P6/S6 (planner wrap + echo inventory). This is 010 Binding (`never paste echo tokens`) with no formula deleted from this SEARCH. |
| **W4** | **(a) ELIGIBLE** | Deletes the writer requirement to emit the frozen rationale line. Matches P3. Existing `Never a "Warm rationale" header` is unchanged. Not a new never-announce. |

### `prompts/style-guide.md`

| ID | Verdict | Why |
|---|---|---|
| **S1** | **(b) STRIKE SIX, DROP** as written | Deletes `only honest answer dismantles the belief` (seed) then adds `Do not prefix it with a how-to-ask instruction or a device name`. That add is the strike-six sentence. |
| **S2** | **(b) STRIKE SIX, DROP** as written | Deletes `runs the trap questions whose only honest answer concedes the point` (the verbatim A ch07/08/09 paste). Then adds `Do not paste a how-to-ask prefix or name the question type in reader prose`. 007/011 silent-execution, not 014 card strip. |
| **S3** | **(b) STRIKE SIX, DROP** as written | Same formula out; `then ask. The question is the next sentence, unprefixed` in. Unprefixed = prefix ban. |
| **S4** | **(b) STRIKE SIX, DROP** as written | Checklist currently *is* the pasteable formula. Deleting it is (a). REPLACE `without a prefix that names the question, and without a how-to-ask instruction in the prose` is (b). |
| **S5** | **(b) STRIKE SIX, DROP** as written | B5 operator 3: 011 already added `without naming, announcing, or labeling`. The remaining pasteable first clause (`only honest answer concedes the point`) is a real seed. REPLACE `Do not prefix it with a how-to-ask instruction or a device name` is strike six. |
| **S6** | **(a) ELIGIBLE** | B2 planner sheet: debut pin plain, echo ID only, never markdown backticks. Matches P2. Not reader-prose silent-execution. |
| **S7** | **(a) ELIGIBLE** | B5 operator 11: headline only; rationale is not a second frozen slogan. Matches P3. Planner/spine seed, not “never announce.” |
| **S8** | **(a) ELIGIBLE** | B10 anatomy item 4: same headline-only. Matches W4. |

**S1–S5 eligible rewrite (not as written, not on apply-list):** delete the pasteable “only honest answer / trap questions” clause; replace with Ask / the question is the next sentence / Carr asserts and asks. Do **not** add do-not-prefix, unprefixed, name-the-question-type, or how-to-ask-in-the-prose. That rewrite is 014 seed subtraction. As written, DROP.

**W1 eligible rewrite (not as written):** `pose two or three questions whose only honest answer concedes the point` → `ask`. Leave the existing silent-execution sentence untouched. Do not insert `unprefixed`.

---

## Restage-as-semantic-job (not ID) vs 002–004 and 014 P2

**NO.**

| Prior | What it bound |
|---|---|
| **002** | Debut-once / echo-by-token on mantra, scene **IDs** (S-xx), evidence **IDs** (E-xx). |
| **003** | Compact-card **job re-own**: “A card may never re-own a settled verdict for a second full demolition. Settled evidence rows and settled jobs are invoked by ID token only.” Job field: if the verdict already landed, token-only echo by ID, never a second enacted demolition. |
| **004** | Scene-bank **ID** debut-once; later cards token-echo only. |
| **014 P2** | Scene **ID**: one full staging; later cards cite **that ID** as a one-phrase token echo, “never a second staging or restated full job.” |

003 already *was* semantic-job (settled jobs / settled verdicts), not merely ID lists. 014 P2 already bans restated full job, scoped to the cited scene ID. 015 P4’s tack-on `or restage a debuted evidence job` is 003’s sentence in five words. It does not bind cinema / boiling-frog / rescuer / restrict-binge **even with a fresh scene ID** — which is the mutation 003→004 named (`plan-skill edits that only constrain evidence/job fields leave scene-bank re-assignment free`).

REVERT is not a ban: 003 wording is eligible under census judges **with a new mechanism**. This is not one. Do not sell P4’s restage clause as 015’s mechanism. It is also not this hypothesizer’s KEEP object (noted factory-speech). Drop it.

---

## Packet as a whole

The hypothesizer packet **as written** still load-bears silent-execution: S1–S5 and W1 are the predicted trap-question-label path (`Do not prefix` / `unprefixed`); W3 is a new writer never-paste. That is 007/011/010 again, sixth strike on the same approach. **NO-GO as written.**

The surviving hunks are 014 Voice-operator strip on the next card seeds (evidence-limit workshop, backtick echo inventory, frozen I-04 rationale) plus matching reviewer gates. That is not silent-execution. Apply those only.

Do not apply S1–S5/W1/W3 “so the batch is complete.” That would make the applied packet silent-execution again.

---

## apply-list (survive as written)

P1, P2, P3, P4 (without `or restage a debuted evidence job`), P5, P6, P7, P8, P9, R1, R2, R3, W2, W4, S6, S7, S8

**DROP:** W1, W3, S1, S2, S3, S4, S5

**NO-GO** the hypothesizer packet as a whole — still silent-execution.
