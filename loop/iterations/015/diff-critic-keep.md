# Diff critic 1 KEEP — applied 015 prompts

**Verdict: GO.** No remaining SEARCH/REPLACE.

Read `git -C /home/kab/quit-sugar-iter-015 diff -- prompts/` against `critic-consensus.md` / `critic-keep.md` / `apply-log.md`. Prompt hunks match the merged packet. W3 is absent. No sixth-strike never-announce / do-not-prefix landed. Both KEEP objects have a chance. No SEARCH landed wrong.

`.env` and `loop/state.md` are also dirty in this worktree. They are campaign state, not this packet. Prompt scope is the four files in `apply-log.md`.

---

## Seed subtraction (factory-speech)

All hypothesizer seed hunks the merged packet kept are on disk, in the right paragraphs:

| Hunk | Landed |
|---|---|
| **P1** | Cards cite evidence IDs only; no permitted/prohibited/SUPPORTED/must-state paste. |
| **P2** | Debut pin plain text; echo ID only; no backticks; no multi-token inventory. |
| **P3** | Frozen instruction = ALL-CAPS headline only; no second slogan / “miss one number” coda. CA-01 unsplit (clinical tails stay off instruction rows). |
| **P5** | Compact-card evidence = IDs only; limits stay in the ledger. |
| **P6** | Compact-card mantra matches P2. |
| **P7** | Guardrails: never device-name trap-question; never “use SUPPORTED limits” as a card instruction. |
| **P8** | Do-not-put list extended with evidence workshop, backtick tokens, trap-question device names. “A card is a room” is planner framing, not a writer ban. |
| **P9** | Normalization law, same strip. |
| **R1** | Do not require limits-on-card; IDs that resolve; debut pin only (not echo pins). |
| **R2** | Blocks the stripped seeds. Does not try to be the job-restage bind (that is R-job). |
| **R3** | Absence of limits-on-card / echo pins is not a blocker. |
| **W1** | SEARCH = hypothesizer W1 SEARCH. REPLACE = formula deleted → `ask`. Existing “never name, count, announce, or stage-direct” untouched. No `unprefixed` / do-not-prefix. |
| **W2** | Writer resolves ledger IDs; no permitted/prohibited recitation. Existing “Never narrate study design” stays. |
| **W4** | Headline at climax; spoken clause is ordinary speech, not a second frozen slogan. |
| **S1–S5** | Pasteable “only honest answer / trap questions” formula gone. Replacements are Ask / the question is the next sentence / Carr asserts and asks. No do-not-prefix, unprefixed, name-the-question-type, or how-to-ask-in-the-prose. |
| **S6–S8** | Debut pin plain / echo ID only; headline-only instruction. |

`prompts/` has zero hits for `only honest answer`, `unprefixed`, `do not prefix`, `how-to-ask`, `never markdown backticks around the token`, `consecutive inventory`. Residual “Socratic trap question” strings are planner/reviewer **card** do-not-put (P7/P8/R2), not a writer prefix ban.

---

## Semantic job restage (book-arc re-argument)

Hypothesizer P4 as written (`restage a debuted evidence job`) did **not** land. Critic-keep P4 REPLACE landed verbatim in Arc/length:

> …a reserved job is not a later restage of a debuted demolition… rather than repeat a plan-wide inventory or restage a debuted **argumentative job** under a new scene ID.

Extra MUST-DO hunks landed in the unique SEARCH sites:

- **P-job** — scene bank: “An argumentative job debuts on exactly one card; a later card must not re-own that job under a new scene ID.”
- **P-scene** — compact-card scene field: “never a new scene ID whose job is a settled demolition.”
- **R-job** — whole-book walk (not writer-facing authority): “restage a settled argumentative job under a new scene ID.”

This is the unpaid both-books class (cinema restage under a new SC-ID, frog on two cards, rescuer, restrict-binge). 014 already token-echoed IDs; this binds the **job**. No writer “do not restage” sentence added. No new audit table. No named cinema/frog strings added to prompts. Pre-existing style-guide “boiling frog” (Burgeon catalogue) and “do not restage settled scenes” (after-vow life chapter) were not this batch.

---

## Sixth-strike / W3 / do-not-prefix

**W3 DROPPED.** Writer frozen-token sentence is still the 014 original (`never a graft… and never re-argued`). No “never markdown backticks around the token, never a consecutive inventory of the same slug.”

W1/S1–S5 applied as consensus rewrite, not hypothesizer-as-written: formula out; `ask` / next-sentence in; hypothesizer `unprefixed` / “Do not prefix” / “Do not paste a how-to-ask prefix” / “without a prefix that names the question” **not inserted**. S5 also dropped the 011 leftover “without naming, announcing, or labeling” instead of replacing it with a new prefix ban — extra subtraction, still eligible.

Existing Binding silent-execution sentences were not extended. No writer restage sentence. No inhabit/world re-run. No 20-room clone.

---

## Both KEEP objects have a chance

Census KEEP this batch = **both** in both books:

1. Voice noted `factory-speech` (014: 17 / 38) — seed strip of evidence-workshop paste, backtick echo inventories, trap-question formula, I-04 frozen coda. Cards no longer assign that narration. Writer is not told a new never-announce.
2. Book-arc noted `re-argument` (014: 5 / 4) — P4 rewrite + P-job + P-scene + R-job subtract re-assignment of a settled demolition under a new scene ID. Reviewer will BLOCK that restage.

A factory-speech drop with re-argument still at 5/4 would not KEEP; a re-argument drop with factory-speech still at 17/38 would not KEEP. The packet attacks both. Confirming-only (journey re-argument, trap-question-label) are not the gate.

Downstream (orchestrator, not a hunk): regenerate the 15-chapter plan. 014 cards still paste SUPPORTED limits, backtick echoes, I-04 safe-rationale, and CH-12 still re-owns CH-04. These diffs are not a no-op **if** that regeneration runs. They are a no-op if it does not. That is already in critic-keep; it is not a prompt SEARCH miss.

---

## SEARCH uniqueness

Each merged SEARCH hit once, in the intended section:

- P1 ledger; P2 mantra sheet; P3 instruction spine; P4 argument-bearing contract; P5–P7/P-scene compact-card bullets; P8 semantic-authorities paragraph (rest of paragraph intact); P9 normalization; P-job scene bank.
- R-job whole-book cumulative walk; R1+R2 writer-facing authority (one git hunk, both replacements present); R3 explicit non-blockers.
- W1+W2 adjacent Binding bullets (one git hunk); W4 anatomy item 5; W3 not in the diff.
- S1 §5.8; S2 §9 Always; S3 §9 Never; S4 §12 checklist; S6 B2; S5 B5 op 3; S7 B5 op 11; S8 B10 item 4.

No hypothesizer P4 “evidence job” leftover. No double-apply. No edits inside `prompts/` outside those four files.

---

## MUST-NOT (held)

CA-01 unsplit. Willpower Method kept. No judges/PROGRAM/models/`research-agent.md`. IN THIS CHAPTER stays. No Maya. No CA-SAFE title. No 20 GSBS rooms.

---

## Remaining SEARCH/REPLACE

None.
