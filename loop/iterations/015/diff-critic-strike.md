# Diff critic 3 — APPLIED 015 prompt diffs

Clock: 010 writer silent craft → 011 style-guide silent operators → 012 residual → 013 packet hygiene → 014 card Voice-operator strip. Learnings name residual **5th time**. Another never-announce / never-paste / perform-silently / do-not-prefix **writer** sentence is strike six.

Test per changed sentence: **(a)** seed subtraction of a speakable formula / card paste, same move as 014 Voice-operator strip — **ELIGIBLE**. **(b)** another “Never announce / never paste / perform silently / do not prefix” writer sentence — **STRIKE SIX, DROP or rewrite**. Do not invent new never-announce.

Scope: `git -C /home/kab/quit-sugar-iter-015 diff -- prompts/` after merged apply (`critic-consensus.md`, `apply-log.md`). Hypothesizer-as-written is not the packet.

---

## Per hunk (applied text)

### `prompts/master-plan-skill-v2.md`

| ID | Verdict | Why |
|---|---|---|
| **P1** | **(a) ELIGIBLE** | Planner: cards cite evidence IDs only; never paste permitted/prohibited/SUPPORTED/must-state workshop onto the card. Same move as 014 P3/P8. Not a writer never-announce. |
| **P2** | **(a) ELIGIBLE** | Strip backtick wrap + echo-card quote inventory. Debut pin stays once, plain text. Seed of frozen-token paste is the card. |
| **P3** | **(a) ELIGIBLE** | Subtract the frozen second slogan from the spine row (headline only; no “miss one number” coda). Spoken clause is ordinary owning-chapter speech, not a second frozen token. CA-01 unsplit. |
| **P4** | **(a) ELIGIBLE** | critic-keep REPLACE, not hypothesizer “evidence job.” Evidence-ledger IDs only (P1 seed). Reserved job is not a later restage; adjacent cards must not restage a debuted **argumentative** job under a new scene ID. Planner re-assignment subtraction. No writer “do not restage.” |
| **P5** | **(a) ELIGIBLE** | Compact-card duplicate of P1. Limits stay in the ledger. No restage tack-on here. |
| **P6** | **(a) ELIGIBLE** | Compact-card duplicate of P2. Debut pin plain; echo ID only. |
| **P7** | **(a) ELIGIBLE** | Extends 014 card do-not-put: no device name, no “use SUPPORTED limits” as a card instruction. Planner-facing. |
| **P8** | **(a) ELIGIBLE** | Same 014 P8 list, next seeds (evidence workshop, backticks, trap-question device names). “A card is a room…” is planner metaphor, not a writer silent-execution sentence. |
| **P9** | **(a) ELIGIBLE** | Normalization of the same strip. Debut plain-text pin remains the exception. |
| **P-job** | **(a) ELIGIBLE** | Scene bank: argumentative job debuts on exactly one card; later card must not re-own that job under a new scene ID. Card seed subtraction of job restage. Not a writer sentence. |
| **P-scene** | **(a) ELIGIBLE** | Compact-card scene field: never a new scene ID whose job is a settled demolition. Same planner bind. |

### `prompts/master-plan-reviewer-v2.md`

| ID | Verdict | Why |
|---|---|---|
| **R1** | **(a) ELIGIBLE** | Stops requiring the paste P1/P4 subtract. Debut pin only. Same move as 014 R3. |
| **R2** | **(a) ELIGIBLE** | Blocks the seeds this batch strips if they reappear. 014 R2 pattern. Planner/reviewer, not writer Binding. |
| **R3** | **(a) ELIGIBLE** | Absence of limits-on-card / echo pins is not a blocker. |
| **R-job** | **(a) ELIGIBLE** | Block restage of a settled argumentative job under a new scene ID. Reviewer gate for P-job. No writer restage sentence. |

### `prompts/chapter-writer.md`

| ID | Verdict | Why |
|---|---|---|
| **W1** | **(a) ELIGIBLE** | Applied REPLACE is formula deletion only: `pose two or three questions whose only honest answer concedes the point` → `ask`. Existing `Perform every rhetorical move silently` is untouched. No `unprefixed`, no do-not-prefix insert. This is the critic-strike eligible rewrite, not hypothesizer-as-written. |
| **W2** | **(a) ELIGIBLE** | Deletes the writer-prompt seed `Internally hold every evidence grade… permitted inference, prohibited inference…` and points the writer at card IDs → ledger. `Never narrate study design, grades, or methods` already existed; adding `permitted/prohibited cells` names P1’s stripped cells. Not a new silent-execution program. |
| **W3** | **not in diff** | DROPPED. No additive `never markdown backticks around the token, never a consecutive inventory of the same slug`. Seed remains P2/P6/S6. |
| **W4** | **(a) ELIGIBLE** | Deletes the writer requirement to emit the frozen rationale line. Matches P3. Existing `Never a "Warm rationale" header` is unchanged. Not a new never-announce. |

### `prompts/style-guide.md`

| ID | Verdict | Why |
|---|---|---|
| **S1** | **(a) ELIGIBLE** | Deletes `only honest answer dismantles the belief`. REPLACE is `Ask. The question is the next sentence.` No do-not-prefix, no device-name add. Consensus eligible rewrite. |
| **S2** | **(a) ELIGIBLE** | Deletes `runs the trap questions whose only honest answer concedes the point`. REPLACE is Carr asserts and asks / questions for the reader’s own evidence. No how-to-ask / name-the-question-type. |
| **S3** | **(a) ELIGIBLE** | Same formula out; `then ask. The question is the next sentence.` No `unprefixed`. |
| **S4** | **(a) ELIGIBLE** | Checklist pasteable formula deleted; assert then ask. No prefix / how-to-ask-in-the-prose. |
| **S5** | **(a) ELIGIBLE** | Formula out; `ask; the question is the next sentence. Perform it in flat Carr voice.` Drops 011’s `without naming, announcing, or labeling`. No do-not-prefix insert. “Perform it in flat Carr voice” is remainder, not a new perform-silently sentence. |
| **S6** | **(a) ELIGIBLE** | B2 planner sheet: debut pin plain, echo ID only, never markdown backticks. Matches P2. Not reader-prose silent-execution. |
| **S7** | **(a) ELIGIBLE** | B5 operator 11: headline only; rationale is not a second frozen slogan. Matches P3. |
| **S8** | **(a) ELIGIBLE** | B10 anatomy item 4: same headline-only. Matches W4. |

No remaining (b) sentence in the applied diff. Grep of added text: no `unprefixed`, no `Do not prefix`, no how-to-ask, no writer never-paste-echo, no new perform-silently.

---

## Packet as a whole

Hypothesizer-as-written was NO-GO (S1–S5/W1 unprefixed / do-not-prefix; W3 additive writer never-paste). Applied packet dropped those and used the eligible rewrites.

Surviving hunks are 014 Voice-operator strip on the next card seeds (evidence-limit workshop, backtick echo inventory, frozen I-04 rationale) plus matching reviewer gates, plus planner job-ownership (P4 / P-job / P-scene / R-job). That is not silent-execution.

“The question is the next sentence” is the formula leaving (ask now), not a sixth Binding sentence.

---

## Verdict

**GO**
