# Diff critic 3 — second apply (Carr job-ownership remaining hunks)

Clock: 010 writer silent craft → 011 style-guide silent operators → 012 residual → 013 packet hygiene → 014 card Voice-operator strip. Learnings name residual **5th time**. Another never-announce / never-paste / perform-silently / do-not-prefix **writer** sentence is strike six.

Test per changed sentence: **(a)** seed subtraction of a speakable formula / card paste, same move as 014 Voice-operator strip — **ELIGIBLE**. **(b)** another “Never announce / never paste / perform silently / do not prefix” writer sentence — **STRIKE SIX, DROP or rewrite**. Do not invent new never-announce.

Scope: `git -C /home/kab/quit-sugar-iter-015 diff -- prompts/` after the remaining Carr job-ownership apply (`diff-critic-carr.md` remaining SEARCH/REPLACE). Prior strike: `diff-critic-strike.md` (first apply GO). This pass re-tests the new P4 / P-job / P-scene / R-job sentences and reconfirms W3 / W1 / S1–S5.

---

## Remaining hunks (Carr job-ownership)

These four sentences are PLAN/REVIEWER job-ownership. They bind what a later **card** may own. They are not writer Binding, not silent-execution, not never-announce.

| ID | File | New text | Strike test |
|---|---|---|---|
| **P4** | `prompts/master-plan-skill-v2.md` | `The job that debuts is what this chapter makes true. A later card must not make that same demolition true again under a new encounter or a new scene ID; it may name the already-true picture in one breath.` | **Not (b).** Planner: later cards may token-name a settled demolition; they must not re-own it. No “Never announce / never paste / perform silently / do not prefix.” Not a writer sentence. |
| **P-job** | `prompts/master-plan-skill-v2.md` | Job = demolition the owning card makes true, not a new scene’s label; debut once; later card must not re-own under a new encounter or new scene ID; two scene IDs must not carry the same demolition; cards before debut do not cite the ID. | **Not (b).** Scene-bank planner law. Same class as first-apply P-job. No writer restage sentence. No “Perform restage silently.” |
| **P-scene** | `prompts/master-plan-skill-v2.md` | Compact-card scene field: never a new scene ID **or** a new encounter whose work is a demolition a prior card already made true. | **Not (b).** Planner compact-field bind matching P-job. |
| **R-job** | `prompts/master-plan-reviewer-v2.md` | `Block a later card that re-owns a demolition a prior card already made true, under a new encounter or a new scene ID.` | **Not (b).** Reviewer gate for P4/P-job. Off the adjacent list; later-card, not writer. Post-vow second-manual sentence untouched. |

“Must not make that same demolition true again” is card-assignment subtraction (who owns the job), not a sixth writer never-announce. “It may name the already-true picture in one breath” is planner echo permission, not “never announce the echo.”

---

## Reconfirm (first-apply writer/style hunks still hold)

| ID | Strike-six check |
|---|---|
| **W3** | **Still absent.** Not in `git diff -- prompts/chapter-writer.md`. No additive `never markdown backticks around the token, never a consecutive inventory of the same slug`. |
| **W1** | **No do-not-prefix.** Applied REPLACE is formula deletion only: `pose two or three questions whose only honest answer concedes the point` → `ask`. Existing `Perform every rhetorical move silently` is untouched context. No `unprefixed`. |
| **S1** | **No do-not-prefix.** `Ask. The question is the next sentence.` No how-to-ask / device-name add. |
| **S2** | **No do-not-prefix.** Carr asserts and asks. No name-the-question-type. |
| **S3** | **No do-not-prefix.** `then ask. The question is the next sentence.` No `unprefixed`. |
| **S4** | **No do-not-prefix.** Assert then ask. No prefix / how-to-ask-in-the-prose. |
| **S5** | **No do-not-prefix.** `ask; the question is the next sentence. Perform it in flat Carr voice.` 011’s `without naming, announcing, or labeling` stays dropped. Remainder, not a new perform-silently sentence. |

Grep of added writer/style text: no `unprefixed`, no `Do not prefix`, no how-to-ask, no writer never-paste-echo, no new perform-silently, no writer “do not restage.”

---

## Packet as a whole

First-apply surviving hunks unchanged (014 Voice-operator strip on next card seeds + eligible W1/S1–S5 formula deletion). Second-apply remaining hunks are plan/reviewer job-ownership only. That is not silent-execution. Not strike six.

---

## Verdict

**GO**
