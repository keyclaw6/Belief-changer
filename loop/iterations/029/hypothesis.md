## Failure evidence

Census class: **`factory-speech`** (voice, noted) in both **027 accepted** books: A 13 / B 16. Blocking 0/0. ≥8 both. Comparison `missing` is not in both (1/0). No blocking in belief/journey/arc. `willpower-lexicon` 28/36 names/attacks the Willpower Method — not PRIMARY.

028 tried operator 11 (card-header paste ban) and was INCONCLUSIVE: 13→18 / 16→10. Both 028 books still opened every chapter with `**IN THIS CHAPTER**` because §B10 anatomy item 1 still required that header. 027 workshop/seed-later strings did not recur. A rose on a different leak (ch13 formula-repetition).

## Root cause

**PRIMARY — style-guide `prompts/style-guide.md` §B10 chapter anatomy item 1.** The full-length architecture tells the writer, "every chapter, no exceptions," to emit the workshop header `IN THIS CHAPTER`. Sentence-operator bans cannot override a required anatomy slot. 028 proved that.

## Targeted fix

**Change 1 (PRIMARY) — `prompts/style-guide.md` — class `factory-speech` (voice) — component style-guide (§B10 anatomy item 1).**

Replace the required `"IN THIS CHAPTER"` header with **Opening pictures**: spoken names of the rooms/pictures, not the workshop header. No operator-11 replay. No judge edit. No 020–024 wording.

**Why this component:** 028's lesson is that B10, not operator 11, is what forces the header the voice judge notes as factory-speech.

## Predicted impact

**PRIMARY:** voice `factory-speech` falls in BOTH books versus 027 (A 13, B 16) by ≥2 at the same n. **Secondary:** `copied-mannerism` may fall if the header was double-counted.

**Possible regressions:** dropping the GSBS-shaped preview label could thin the chapter-open inventory (Carr uses that header). Spoken picture names must remain. Length floor ≥48000. A 028-style formula-repetition leak can still raise the class in one book.

**How we'll know it worked:** `factory-speech` (noted + blocking) must fall by ≥2 in BOTH replicates versus 027. Grep for "IN THIS CHAPTER" is not KEEP.
