# Loop Prompts

Prompts used by the auto-tuning loop orchestrator.

| File | Role |
|------|------|
| `trace-analyzer.md` | Maps judge gaps to factory components |
| `hypothesizer.md` | Proposes one change with 4-field contract |

These are used by the loop orchestrator between iterations.
The factory prompts themselves live in `prompts/` (the tuning surface).
