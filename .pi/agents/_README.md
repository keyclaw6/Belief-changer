# v2 agent wrappers

Wrappers name roles and point to active contracts. They do not choose a substitute evaluator or bypass frozen task/result validation. Use factory/config.json and explicit operator authorization. Read docs/FACTORY-V2.md for stdin adapters and imported response metadata. Historical wrappers are archived.
