# Production Books

Books **generated by the Belief-Changer pipeline** live here — one subfolder per book (research, master plan, chapters, and final outputs).

Empty until the first book is produced.

> This folder replaces the former root `books/`, which held the *reference* source texts. Those now live in [`/analysis/reference-books/`](../analysis/reference-books/) alongside their analyses.
