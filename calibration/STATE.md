# Calibration state

This is the root orchestrator's concise recovery checkpoint, not an experiment log or a substitute for frozen evidence. Read it first after compaction or handoff, then verify it against the active manifest/report, final ledger row, and `git log -5` before delegating work.

## Universal goal and loop

Build one behavior-agnostic factory that turns a subject, desired belief change, and optional consented experience into research, framing, a master plan, and a complete original Easyway-quality book. Quit-sugar and *Good Sugar Bad Sugar* are calibration specimens only; the reference remains evaluator-only, and untouched holdout performance is the proof against overfit.

Use the simplest empirical loop: freeze one generic hypothesis and its inputs; produce the complete Chapters 1–3 first-draft batch before reviewing or revising any chapter; measure it mechanically, internally, and with independent blind judges covering distinct dimensions; record verdict, causal rivals, and autopsy; then keep, revert, or replace the one lever. Metrics and internal reviews can block promotion but cannot suppress target-panel diagnosis when three readable artifacts exist. Repeat to Chapter parity, full-book parity and surpass, then zero-tuning holdout generalization. Never tune one chapter in isolation.

## Roles

- The root context is the brain: preserve intent, choose the single next decision, commission bounded work, reconcile evidence, and own what enters the run record.
- Subagents are the hands: perform every model call, review, test, edit, and verification in scoped fresh contexts, then return artifacts and evidence. They do not silently change direction or experimental law.
- Use the HARNESS direction auditor at its named checkpoints. Keep accepted decisions in this file and the run record, never only in a context window.

## Current checkpoint — 2026-07-12

- Branch: `calibration-lab`.
- Active run: `run-012`; H-049 is causally **REFUTED** under its preregistered rule, while the product verdict remains **UNJUDGED**. Preserve v2 FAIL, v2.1 identical PASS/degraded FAIL, and v2.2 identical PASS/degraded FAIL. V2.2 ran only under the frozen `ab13de1` configuration; its degraded failure stopped product inference. Prospective v2.3 is now preregistered but not implemented or run.
- Last accepted product: `production-books/quit-sugar/chapters/chapter-01.md` from run-002 and `chapter-02.md` from run-004. Runs 005–012 have promoted no later prose.
- Frozen generic commissioner: commit `23ca037`; SHA-256 `b1eabd112ab9c1c6e67284ab7bae27c512bf7ac3affb3be02877343c829e1c18`.
- Frozen grounded commissions: commit `4f4eda4`; C-01 `bb11fef...`, C-02 `8eed6fb5...`, C-03 `08c52093...`. The source-fidelity gate accepted all three at commit `bfffe90`.
- Frozen first-draft batch: C-01 R1 at commit `8258f9e`, SHA-256 `8c1cdbe...`, 2,252 `wc` words; C-02 R1 at commit `4f933dc`, SHA-256 `f28d4d...`, 4,162 words; C-03 R1 SHA-256 `835e3273...`, 3,306 words. None is accepted product.
- Objective batch measurement is complete at `calibration/runs/run-012/metrics/batch/`: real exit 2, three overlapping non-mantra 12-gram failures from one duplicated M-01-adjacent construction across C-01/C-03, zero mantra failures, zero reference overlap, and zero warnings. Instrument words are C-01 2,199, C-02 4,093, C-03 3,241; all three miss their plan-budget ±20% bands. Craft and target-comparison ratios remain diagnostics and cannot suppress the panel.
- Grounded specialist measurement is complete: three independent native Sol-ultra contexts returned MATERIAL DEFECT for C-01 (13 findings), C-02 (6), and C-03 (8). Exact passages and authority boundaries, raw events, usage, thread IDs, stderr, and the batch index are under `calibration/runs/run-012/audits/grounded-*`. All commissions were faithful, yet material evidence invention appeared in 3/3 first drafts, satisfying H-049's causal-refutation condition. This does not decide blind product quality.
- The complete run-012 C-01-C-03 first-draft batch remains frozen. Do not revise a chapter or change a factory asset before a valid controlled blind panel is recorded, despite the objective and grounded failures. Judge-instrument resolution under the new-baseline discipline is the only intervening work authorized by this checkpoint.
- Preserved partially non-authoritative measurement: `calibration/runs/run-012/reviews/chapter-01-r1.md` records a reviewer that saw the canonical plan without the frozen C-01 source-grounded commission. Its evidence-grounding and attribution findings, and therefore its overall `REVISE`, cannot gate continuation or score H-049. Preserve only independently checkable craft, anatomy, chapter-job, length, direct-address, and sentence-pulse observations as diagnostics.
- Native v2 identical-text control: complete 20/20 raw judgments, 20 unique fresh native Sol-ultra threads, 10/10 collapsed observations, zero invalid judgments, and a complete identity/role/target/order matrix. All 20 raw verdicts were ties; every raw A/B score and critical set was exactly equal. Each repetition for a role/target had the same input SHA-256, yet 4/10 collapsed observations changed absolute shared critical labels across fresh contexts. V2 misclassified that absolute calibration drift as order bias and therefore **FAILED** under its frozen rule. Same-text absolute score shifts reached three points but were diagnostic rather than the cause of failure. Preserve all evidence under `calibration/runs/run-012/judgments/controls/identical/`; never reclassify or overwrite it.
- V2.1 identical control: **PASS** from scratch under commit `19fa552`; 20/20 raw judgments, 20 unique threads, 10/10 complete observations, zero invalid, 20 ties, maximum within-call gap zero, and zero comparative instability. Absolute drift remained diagnostic: five label observations, eight score observations, maximum same-text score shift four. Evidence is under `judgments/controls-v2.1/identical/`; summary SHA-256 `7623699f...`.
- V2.1 degraded-reference control: **FAIL** after all 20 fresh contexts. Three otherwise complete final objects contained trailing commas and failed the unchanged strict JSON parser, so the panel was incomplete. All 17 valid raw verdicts preferred the intact reference, but five of seven collapsible observations changed candidate-unique critical labels across repetitions and therefore failed the frozen comparative-stability rule. No retry or reinterpretation occurred. Evidence is under `judgments/controls-v2.1/degraded-reference/`; summary SHA-256 `ebfa0e57...`.
- The v2.2 identical control **PASSED** from scratch: 20/20 first responses strict-valid, 20 unique threads, 20 raw ties, 10/10 complete observations, zero comparative instability, and zero candidate-relative critical-label drift. Repair A and the identical portion of Repair B passed. Summary SHA-256 `f9077e5f...`; preserve it under `judgments/controls-v2.2/identical/`.
- The v2.2 degraded-reference control **FAILED** after a complete 20/20 strict-valid matrix. All 20 raw verdicts preferred the intact reference and every required role-aligned core label was degraded-only in both repetitions, but four of ten collapsed observations changed comparative signatures: two craft observations changed candidate-relative secondary labels and both integrity observations changed the `non_shaming_regard` score-winner sign. Repair A is supported across 40/40 first responses; Repair B is refuted by two relative-label-drift observations. Summary SHA-256 `ca77838c...`; preserve it under `judgments/controls-v2.2/degraded-reference/`.
- The product panel was **NOT RUN** because the v2.2 degraded gate failed. This is an instrument-reliability result, not evidence about H-049 or book quality; no H-050 exists. The complete run-012 prose batch remains frozen and unjudged.
- V2.1 retained the preregistered comparative signature—verdict, per-dimension winner signs, and labels unique to each candidate—plus explicit absolute drift and unioned critical safety. The identical pass establishes comparative symmetry only. The degraded failure shows both strict-output fragility and relative critical-taxonomy instability despite unanimous valid verdict direction; neither rule may be changed post hoc.
- V2.2 Repair A result: **SUPPORTED** — 40/40 first responses were strictly valid with zero retries, repairs, extraction, tolerant parsing, or process failures.
- V2.2 Repair B result: **REFUTED** — all core labels were retained exactly as predicted, but two degraded craft observations changed a candidate-relative secondary label across repetitions. The frozen zero-drift rule cannot be waived.
- V2.2 held v2.1 comparative aggregation, label enums, response fields, unioned critical safety, all score/control/product thresholds, intact/degraded/product materials and degradation, native Sol-ultra model route, exact two identities, fresh-context rule, and 20-cell matrix fixed. Only `--output-schema` transport enforcement and the operational label definitions changed.
- V2.2 evidence is complete and immutable. The runner correctly refuses product use because both Repair B and the degraded semantic gate failed. Do not retry, repair, reinterpret, or change the instrument, prose, or factory assets from this result.
- The v2.3 direction audit found an aggregation-attribution defect, not a reason to reclassify v2.2: replica identities had no differing prompt, model, seed, or state, yet v2.2 paired calls by those arbitrary labels. In the two integrity failures, each order stratum had the same `non_shaming_regard` sign multiset `{-1, 0}`; permuting the order-1 labels changes the identity-paired stability result. All 20 degraded raw verdicts still preferred intact, every raw core anchor was degraded-only, and every raw transform-aligned dimension favored intact. Secondary labels and tie-boundary score signs are therefore uncertainty diagnostics under the prospective pooled design, not vetoes.
- V2.3 changes aggregation and control attribution only. Four raw calls for each role target are pooled as two exchangeable order strata; preference averages the two replicas within each order and then the two order means; roles mean their targets; the equal-role macro is unchanged. Replica labels remain trace metadata. A pass additionally requires at least three of four raw verdicts in {ours, tie} for every target and zero symmetric verdict/rubric contradictions. Strict dominance means higher on every dimension plus a critical-label subset (equality allowed); a unique critical blocker defeats dominance. Failed targets/calls stay reported and aggregated; score variation otherwise remains diagnostic rather than an independent veto.
- All raw critical labels are unioned conservatively across four calls, including labels shared with the reference; zero `critical_ours` has no consensus waiver. Every control and product invocation must have 20 unique fresh threads, exactly two calls per role-target-order, matching input/schema hashes within each stratum, mappings rebuilt from parsed A/B plus order, and configuration matching the frozen baseline and validated controls. Every decision-relevant aggregate and pass/fail result must also be invariant to within-order replica-label permutations after trace-only metadata is normalized.
- V2.3 keeps the v2.2 prompts, schemas, labels, model/ultra native route, structured transport, exact 20-cell matrix, materials and degradation, blindness, existing numerical macro/role thresholds, and union safety fixed. Its fresh controls hard-pass only under HARNESS §9; no v2.2 call is reused. If a control or the permutation invariant fails, there is no v2.4 on the same synthetic specimen: escalate to a different validation family or human benchmark. If both pass, product runs immediately without intervening instrument work.
- Next technical artifact: implement and synthetically test only the v2.3 pooled aggregation/control attribution, including both candidate directions, tie/opposite contradictions, swaps, unique-critical exception, target polarization, mapping reconstruction, matrix invariants, and failed-call preservation; then freeze its hash before inference. Next product artifact: the frozen run-012 target panel immediately after two fresh v2.3 controls pass. Product judging remains forbidden until then; no H-050 or factory lever may be formed from an unvalidated instrument.
- Judge baseline: v2 remains FAIL; v2.1 and v2.2 each remain identical PASS/degraded FAIL and invalid for product use. V2.3 is prospective and non-comparable. No earlier baseline is reclassified or reused.
- Native v2.2 transport worked as designed across both controls: 40 isolated Sol-ultra contexts, 40 unique threads, zero process failures, and no provider API key inherited. Combined usage was 1,242,519 input tokens (233,216 cached), 75,934 output tokens, and 65,729 reasoning-output tokens; usage is descriptive only.
- Historical judge preflight: the operator previously resolved a Gemini-high + OpenRouter-Sol-max matrix, observed `$0.797648511` OpenRouter credit, and correctly made zero judge calls, ran neither control, and created no judgment artifacts. The founder then corrected that route: OpenRouter must never carry a judge call. This is preserved as a zero-inference preflight autopsy, not an active funding requirement.
- External blocker: **none**. The active stop is an internal judge-instrument failure, not funding or access. The approximately `$0.979` key allowance remains untouched for permitted Opus chapter writing; OpenRouter is otherwise allowed only for DeepSeek research. C-02's completed response remains frozen with transparent response-metadata loss; C-03 used absolute artifact paths and retained complete provider metadata without resampling C-02.

## Invariants

- Tune generic factory assets, never sugar facts, specimen prose, or a chapter-specific prompt.
- Keep pipeline roles blind to reference text and judge output; judges alone compare with the local reference.
- Optimize for method integrity and quality, not speed, cost, latency, or token use.
- Hold the run-012 shared contract fixed after prose began; do not repair a commission, prompt, threshold, or chapter-specific instruction mid-run.
- Complete the frozen Chapters 1–3 causal sample unless the preregistered run rule itself stops it; one chapter cannot become the tuning loop.
- Writer remains Opus 4.6 with reasoning disabled. Non-writer roles use their allowed strongest reasoning mode.
- OpenRouter is restricted to Opus 4.6 chapter writing and DeepSeek research. GPT/OpenAI, Gemini, Grok, planning, reviewing, auditing, and judging never use it; current product judges are native spawned Sol-ultra subagents.
- Treat invalid or non-comparable measurements explicitly; record why before rerunning anything so false positives and false negatives cannot silently steer the factory.
- Preserve v2, v2.1, and v2.2 exactly as observed. Never repair or reinterpret a failed control after seeing its output; uncertainty diagnostics stay visible and unioned critical safety stays conservative.
- V2.3 may change only aggregation and control attribution. Freeze its implementation before fresh calls; preserve every v2.2 prompt, schema, label, route, matrix, material, threshold, and raw record.

## Update rule

The root decision owner must have this checkpoint updated at every accepted artifact, measurement, or decision boundary and before deliberate handoff. After unexpected compaction, recovery starts by reading this file and verifying it against the empirical records. Replace current facts here rather than growing a diary; chronology belongs in the run report and ledger. A subagent may write the update only under an explicit bounded commission, and the root must reconcile it before delegating the next action.
