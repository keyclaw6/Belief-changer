# init-baseline-specs — tasks

- [x] Create openspec/ tree + .openspec.yaml + proposal.md
- [ ] Author 4 domain delta specs (method-integrity, book-pipeline, publishing, languages)
- [ ] Validate with `openspec validate init-baseline-specs --strict` (if CLI available) else structural self-check
- [ ] Founder reviews the change
- [ ] Archive: apply deltas into openspec/specs/<domain>/spec.md + git mv change folder to openspec/changes/archive/YYYY-MM-DD-init-baseline-specs/, one commit
