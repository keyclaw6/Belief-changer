# Decision Record — 20260812-092736-loop-dryrun

Consumed audit: `reviews/orchestration-audit/20260812-092736-loop-dryrun/`
(agent-orchestration-auditor; 6 workflow dry-runs + skeptical verifier).
Integrator: `agent-orchestration-audit-integrator` v1.1.0.
Mode: founder-authorized (the founder directed "make everything proper" and
"merge the findings"). Governing intent re-read: AGENTS.md,
docs/AUTO-TUNING-LOOP.md. No live external effects; verification was static +
throwaway-copy execution only.

Every finding dispositioned below. Fixes shipped in commits `5826ae1`
(harden) — note: the fixes were applied in-session immediately after the audit
at the founder's direction, then independently verified here (cold) before
disposition.

| ID | Sev | Finding | Disposition | Evidence / verification |
|----|-----|---------|-------------|-------------------------|
| F1 | MED | No split-brain / incarnation guard | **APPLIED** | `loop/state.md` Owner run-token + `loop/PROGRAM.md` §0 IN-PROGRESS resume-refusal. Verifier: UNIT-2 VERIFIED (consistent with results.tsv exception). |
| F2 | MED | Writer stage has no completion marker | **APPLIED** | `PROGRAM.md` §4 Step 3 — every writer spawn writes `.loop-work/writer/chapter-NN.exit`; stuck = marker absent + no trace progress across two wakes. |
| F3 | LOW | reuse existence glob spanned all books | **APPLIED** | `research_reuse.sh` scoped to `production-books/<book>/research`. Verifier UNIT-1 case 6 PASS. |
| F4 | MED | config.yaml all-or-nothing research trigger | **APPLIED** | config change reruns research only when the diff touches a research key (researcher_/research_); no diff → fail-safe RERUN. Verifier UNIT-1 cases 2/3/4/4b PASS. |
| F5 | MED | "one commit" KEEP file-set not pinned | **APPLIED** | `PROGRAM.md` Step 7 pins the KEEP/REVERT commit file-set (never `git add -A`), verified by `git show --stat`. |
| F6 | LOW | inbox "oldest first" + drain undefined | **APPLIED** | oldest = lexicographic filename; one note per iteration. `PROGRAM.md` Step 1 + `loop/inbox/README.md`. |

## Notes / non-findings
- The audit also returned healthy-area confirmations (happy path, resume,
  patience-healthy, inbox well-formed/vague, worktree promote): COMPLETES.
- One latent (F3) was already non-triggering via the config path; fixed anyway
  as a wrong-threshold hardening.
- No UNRESOLVED-QUESTION items remained after verification.

## Outcome
All six confirmed findings APPLIED and independently verified. Audit workspace
retired in the cleanup commit. The loop is dry-run-verified end to end.
