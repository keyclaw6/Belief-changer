# Worker shard — AOG-AMB-02 (independent ambiguity interpreter)

- Reviewer: GPT-5.6 Terra medium sub-agent (nickname Mendel), read-only, isolated

## E1 — Miner packet write target
Primary: work order supplies a concrete aggregate bank-file path; miner appends bank entries in that file's existing Markdown format (Banks 1-6/9-10 in lived-experience.md; 7-8 in scientific-evidence.md). Alternative: "packets" = per-URL Markdown packet in research/sources/ with bank files containing references — changes path, cardinality, format, ownership. No supplied work order resolves the exact path.

## E2 — Reddit authorization
Primary: direct Reddit excluded absent explicit authorization; mirrors/archives are authorized alternatives; ≥3-persona criterion does not name direct Reddit, so a run can clear using permitted sources. Alternative: higher-level "must reach Reddit" requires direct Reddit; no authorized miner can fulfill the complete mandate until external authorization — changes routing, termination, scarcity-sufficiency. AGENTS.md:13 and AUTO-TUNING-LOOP.md:129 are the higher-priority language asserting Reddit; role prompt expressly permits mirrors/archives. No repository process grants the authorization.

## E3 — Plan-review cycle limit
Primary: (a) keep cycling — PROGRAM.md:175-177 "until" + no-waive rule exclude acceptance regardless. Alternative: (b) "up to three cycles" is an operational cap; after third BLOCK stop and escalate — changes termination and escalation. Sources do not define a resolution.

## E4 — Writer refusal action code
Primary: `repair_owner_and_regenerate_downstream` is a structured routing label identifying the needed repair boundary, not an obligation to repair+regenerate in the same iteration; PROGRAM.md:209-212 specifies the operational effect (save, no chapter, owner as finding, INCONCLUSIVE). Alternative: same-iteration repair+regeneration — changes authority, writes, routing, termination. Runbook resolves immediate handling toward primary; no explicit subsequent-repair schedule.

## Integrity
- No files written; no services called; did not inspect other interpreters' outputs.
